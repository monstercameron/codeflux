CODEFLUX DEVELOPMENT LOG

Purpose
-------
This tracked ledger records implementation chronology, decisions, validation,
failures, limitations, and the next safe step. It is not a raw transcript and
must never contain secrets or private model reasoning.

Entry fields
------------
Dev-Log:
Date:
Status:
Change-ID:
Request-or-TODO:
Goal:
Assumptions:
Decisions:
Files-or-schemas:
Validation:
Failures-or-discarded-approaches:
Known-limitations:
Next-safe-step:

Entries
-------
Dev-Log: DL-20260731-073
Date: 2026-07-31
Status: Complete
Change-ID: CL-20260731-072
Request-or-TODO: User request to extract the styling from the six
  design/references/ui mockups and move the shipped frontend toward it using
  the existing theming and i18n systems, without breaking changes
Goal: Close the gap between the shipped navy/teal dashboard and the mockups'
  near-black ground with an emerald accent and a typed per-kind palette, at
  the token layer so every surface inherits it without component edits
Assumptions: The mockups are authoritative for visual direction; design/ is
  user-owned and read-only; shell, graphcanvas, and timelineview were held by
  other lanes and could not be edited in this pass
Decisions: Change tokens rather than components, so unowned surfaces inherit
  the palette with no merge surface; extend ColorTokens additively so nothing
  downstream stops compiling; leave GeometryTokens radii alone because an
  existing exact-equality test pins them, and record the divergence from the
  mockups rather than editing an assertion to match
Files-or-schemas: web/frontend/design/tokens.go, contrast.go, kind.go;
  web/frontend/primitives/chip.go, contracts.go;
  web/frontend/timelinecard/presentation.go; matching tests. No schema.
Validation: 76 fixed contrast pairs verified against WCAG AA in dark, light,
  and high-contrast; design, primitives, timelinecard, and i18n tests pass;
  go build, go vet, gofmt, and the js/wasm web build clean; screenshots taken
  at 1920, 1440, 1280, 1024, and 820 wide in both themes and inspected, with
  one grid overflow at 820 found and corrected before acceptance
Failures-or-discarded-approaches: An initial fixed-column verification grid
  overflowed at 820 px and was replaced with RepeatFit/MinMax. Fixing the
  light theme by mechanically inverting dark tokens was rejected; each
  text/surface pair was measured instead, which is how the roughly 1.1:1
  panel-header contrast was found.
Known-limitations: The IconChip primitive is built and tested but not yet
  rendered anywhere, because timelineview was owned by another lane; the
  mapping hook timelinecard.Card.DesignKind() exists for that wiring. Card
  radii remain tighter than the mockups. The running app will not show the new
  palette until it is rebuilt and redeployed.
Next-safe-step: Wire primitives.IconChip into timelineview using
  Card.DesignKind() and the MsgKind* catalog entries, then revisit
  GeometryTokens radii with its pinning test.

Dev-Log: DL-20260731-072
Date: 2026-07-31
Status: Complete
Change-ID: CL-20260731-071
Request-or-TODO: M20-001 through M20-094 and M20-G01 through M20-G04.
Goal: Bind change acceptance to exact risk, diff, validation, review, and evidence
  facts and expose the complete decision path through the production Go/GWC UI.
Assumptions: SQLite and server-observed Git state remain authoritative; caller
  observations cannot establish freshness; unsupported external behavior remains
  contract-checked or runtime-only; the fixed preview stays on port 63131.
Decisions: Compose deterministic risk/profile/test selection with mediated
  validation; invalidate evidence on any diff change; require authoritative
  validation provenance; persist sealed claim-level reports; bind review
  mutations to live diff/report/check observations; perform repair, rollback,
  and patch-preservation effects before recording successful outcomes.
Files-or-schemas: Migrations 000021 through 000024; review, validation,
  testselection, validationbaseline, evidence, acceptance, executor, storage,
  coordinator, transport, generated ReviewService, Go/WASM client resources,
  and GWC diff/evidence/review-action surfaces.
Validation: The decisive M20 package suite passes across acceptance, review,
  validation, baseline analysis, test selection, evidence, executor, storage,
  coordinator, transport, Git effects, all review UI packages, and web client.
  Generation, migration, artifact, WASM, protobuf lint (focused agent gate),
  diff checks, and fixed-port health pass. Focused regressions prove one-line
  stale-diff invalidation, fabricated/invalidated evidence rejection, exact
  observed-diff acceptance, real repair/rollback/preserve effects, authoritative
  task-state events, and claim-scoped guarantee ceilings.
Failures-or-discarded-approaches: Adversarial review rejected caller-supplied
  observed identities, unattested validation IDs, journal-only repair/rollback,
  isolated unmounted UI packages, and an import cycle caused by a Git-owned DTO.
  These were replaced with authoritative observers, effect orchestration,
  production review mounting, and a leaf review DTO.
Known-limitations: Validation readiness intentionally fails closed after process
  restart and must be replanned/re-executed. This is conservative, not reuse.
Next-safe-step: Stop at M20 as requested. Begin evaluation/sample-project work
  only in a later explicit effort; do not start M21 automatically.

Dev-Log: DL-20260731-071
Date: 2026-07-31
Status: Complete
Change-ID: CL-20260731-070
Request-or-TODO: M19-001 through M19-108 and M19-G01 through M19-G04;
  user required the graph to be dynamic, interactive, visually meaningful by
  node purpose, fully observable through the fixed live GWC UI, and tested.
Goal: Deliver a task-scoped, derived, read-only program/execution/evidence graph
  with immutable revisions, bounded SQLite queries, deterministic stable layout,
  accessible interaction, bidirectional chat linkage, and explicit inspection.
Assumptions: Chat remains the control surface; graph identity is distinct from
  revision identity; server-issued project/task/repository IDs bound every read;
  sessions without graph evidence render an honest empty graph; `design/` stays
  user-owned and excluded.
Decisions: Normalize durable task facts through a typed semantic seam rather
  than infer structure from redacted display text; commit graph changes only for
  material facts; keep token deltas revision-free; use schema-versioned immutable
  revisions, typed metadata only, bounded continuation queries, deterministic
  SCC-aware left-to-right layout, and authored Go/GWC SVG interaction.
Files-or-schemas: Graph domain and projection, migration 000020, bounded SQLite
  graph repository, deterministic SCC-aware layout, all six authenticated
  GraphService RPCs, Go/WASM adapters, authoritative GWC SVG viewport, graph
  inspector, chat links, production mounting, and browser/performance evidence.
Validation: Domain, projection, migration, storage query, layout, graphcanvas,
  inspector, coordinator, transport, client, frontend, WASM, vet, generation,
  migration, artifact, and diff checks pass. Initial 300-node layout p95 was
  2.49 ms; 100 sequential patches p95 was 5.53 ms. The fixed-port browser gate
  passes completely, including pan, zoom, fit, reset, selection, responsive
  bounds, GWC navigation, keyboard/focus, modal, and collapsed-pane journeys.
  An ordinary production SendMessage created durable graph revision
  grv_019fba42-a0be-789c-a7d2-eca7d32de82b and exposed the same requirement in
  chat and graph. Mounted Chromium additionally verifies visible SVG geometry,
  keyboard selection, inspector loading, and expansion dispatch.
Failures-or-discarded-approaches: Adversarial review rejected topology commits
  not bound to the exact pending candidate, CSS-pixel transforms applied to a
  fixed SVG viewBox, selection strokes that erased status encoding, dangling
  tabpanel relationships, production-only legacy graph data, stale GWC event
  closures, and numeric SVG attributes that the WASM renderer omitted. The final
  implementation uses stable event handlers and string-valued SVG geometry, with
  mounted-browser regressions for both.
Known-limitations: Graph projection is intentionally derived and read-only; the
  chat journey remains fully usable when the graph pane is collapsed. Browser
  tabs holding an older cached WASM may require a fresh application reload.
Next-safe-step: Complete M20 validation, review, evidence, and exact-diff
  acceptance gates; do not begin M21 until explicitly requested.

Dev-Log: DL-20260731-070
Date: 2026-07-31
Status: Complete
Change-ID: CL-20260731-069
Request-or-TODO: M18-001 through M18-070 and M18-G01 through M18-G05;
  user required uninterrupted completion, a fixed live GWC UI, backend-owned
  state, four-agent implementation, full interaction testing, and marked TODOs.
Goal: Deliver authoritative task projection, seven-state connection certainty,
  idempotent task controls, exact cost/budget presentation, calm recovery, and
  local content-free telemetry through the existing Go/GWC workspace.
Assumptions: The selected server-issued thread/session/task identities remain
  the only authority; unknown pricing and absent recovery evidence must remain
  unknown; the untracked user-owned `design/` tree remains excluded.
Decisions: Keep the fixed server on 127.0.0.1:63131; use GWC resource and
  lifecycle hooks rather than any React model; project only ordered SQLite
  events; add an authoritative TaskService read path; disable mutations while
  sequence certainty is unresolved; retain browser-local drafts across replay.
Files-or-schemas: Frontend session/task reducers, GWC client resources and
  controls, coordinator/transport task reads, frontend telemetry, migrations
  000018 and 000019, browser acceptance fixtures, and directly affected tests.
  Client mutation settlement now covers pause, resume, stop, exact budget,
  preserve patch, reconcile, safe resume, and composer send. The diagnostics
  route now receives the identity-checked mounted session projection and shows
  its content-free last applied durable sequence, including distinct unknown,
  known-zero, replay, live, and gap-repair states.
  The production composer now renders every §27C Draft-through-Rolled-back row
  from `taskprojection.AvailableTaskActions`; implemented commands share the
  mounted callbacks, while later-milestone actions are inert and carry exact
  accessible disabled reasons. TaskView is query metadata only: task identity,
  lifecycle, revisions, budget, recovery, and action availability come solely
  from the accepted session task projection.
Validation: Focused composer/session/task packages pass. Live browser selection
  and a second real composer send passed through the gRPC bridge; its durable
  message event cleared and re-enabled the composer without a page refresh.
  The eight-row production mutation contract test, `go test ./web/...`, the
  js/wasm client build, and `go test ./...` pass. M18-G02 is satisfied for
  pause, resume, stop, and budget change. M18-014 targeted client, shell, state,
  and frontend tests pass; both client and browser-fixture WASM builds pass;
  mounted Chromium verifies sequence 0 through replay/disconnect, sequence 1
  during gap repair, and repaired sequence 3 through replay/live/disconnect.
  Native client/projection/action/composer/primitives tests and both WASM builds
  pass. Mounted Chromium exhaustively verifies all 14 §27C rows, exact action
  membership/order, real supported callbacks, pre-click disabled reasons,
  `aria-describedby`, and keyboard focus retention after activation.
  M18-052 is now backed by a typed, atomically cursor-bound
  GetSessionSnapshot path from SQLite through authenticated gRPC; reconnect and
  invalidation repair re-enter only through ApplySessionSnapshot, while ordered
  delivery enters only through ApplySessionEvent. Focused event, storage,
  transport, coordinator, reducer, session-client, web-client, and WASM checks
  pass, including delayed session binding, crash-boundary backfill, every event
  category, sequence-gap repair, complete review bindings, and metadata-only
  TaskView regression coverage.
  Final production evidence includes 260/260 Chromium checks against the fixed
  127.0.0.1:63131 runtime, visual inspection of the live selected-session
  workspace, a real gRPC-bridge message commit, GWC navigation without document
  reload, responsive rail selection, telemetry deletion confirmation/cancel,
  retained approval command identity, graph controls, and accessibility/focus.
Failures-or-discarded-approaches: The live TaskService GetTask probe exposed an
  unimplemented server method; a composer RPC/event settlement race left the
  first send busy until its GWC dependency included ordered event and attempt
  state. No React or useEffect source exists in the frontend. Rejected adapting
  the current TaskView directly into the session projection: GetTask has no
  atomic session cursor, omits approval, validation, and complete recovery
  facts, and the corresponding production task writes do not yet append those
  session events. Such an adapter would preserve the existing double authority
  while merely hiding it behind ApplySessionSnapshot. Per-row raw event
  closures caused unstable GWC handler identity, so the action surface uses one
  stable value-dispatch handler. Animated disclosure made pointer actionability
  timing flaky in mounted Chromium; the acceptance test now activates focused
  enabled buttons by keyboard and asserts the resulting focus and invocation.
  The final browser loop also corrected stale fixture assumptions: root
  restoration is stateful, mutation checks select a durable session, graph fit
  has its own computed zoom, rapid settlement need not expose a busy frame, and
  responsive thread-rail closure is verified through its real control.
Known-limitations: The graph shown for sessions without authoritative graph
  data is intentionally empty rather than fabricated. Later M19 work supplies
  derived graph revisions, queries, and accessible interactive projection.
Next-safe-step: Commit and push M18 with its complete evidence, then continue
  M19 graph projection, query, layout, interaction, inspection, and benchmarks.

Dev-Log: DL-20260731-069
Date: 2026-07-31
Status: Complete
Change-ID: CL-20260731-068
Request-or-TODO: M17-001 through M17-115 and M17-G01 through M17-G04; user
  required a fixed-port, fully observable GWC UI, backend-owned durable work,
  working controls, real routing, E2E inspection, adversarial refinement, and
  marked TODOs.
Goal: Deliver the Layer 13 resumable conversation journey on authoritative
  SQLite, coordinator, RPC, and ordered-session boundaries while keeping the
  browser a typed projection and command surface.
Assumptions: A selected workspace identity is server authority, not browser
  state; the user-owned untracked `design/` tree remains excluded; local
  preview data must never claim durable mutation success.
Decisions: Add real ThreadService and SessionService composition over SQLite
  and EventHub; bind every thread to one session; lift authoritative selection
  into the root; page newest/older thread events and join replay/live delivery
  by stable sequence; retain preview only when bootstrap has no authorized
  workspace; disable unsafe offline composer actions; preserve pending approval
  state and idempotency keys after transport failure; author all presentation,
  routing, canvas interaction, responsive behavior, and accessibility in Go
  with GoWebComponents v5.
Files-or-schemas: Protobuf thread/session resources and generated Go; event
  registry; migrations 000016 and 000017; SQLite thread/session repositories;
  coordinator and transport services; browser bootstrap and RPC bridges; GWC
  composer, thread rail, timeline/card/view, graph canvas, shell, primitives,
  routes, design tokens, and exhaustive browser/reducer tests.
Validation: The live server at `127.0.0.1:63131` passes 260 Chromium checks
  spanning routes, pagination, replay, modals/drawers, keyboard and focus,
  approval failure, offline mutation safety, graph inspection, responsive
  first-run, and all mounted M17 surfaces. `go test ./... -count=1`, `go vet
  ./...`, generation, lint, migration, artifact, js/wasm build, diff, and
  source secret checks pass.
Failures-or-discarded-approaches: Rejected popovers that imposed modal
  background state, controlled search input that dropped characters, exact
  serialized float assertions, cross-scenario browser state leakage, fake
  approval settlement after a failed bridge call, and disabled-control clicks.
  An older disposable preview database was preserved after its unreleased
  migration checksum diverged; a fresh artifact database validates the final
  schema.
Known-limitations: The currently running disposable profile has no selected
  workspace record, so its UI correctly reports Local Offline and disables
  durable mutations until an authorized workspace is opened. Workspace-opening
  product flow and complete live task controls remain later milestone work.
Next-safe-step: Commit and push M17, verify the public workflow, then implement
  M18 connection, projection, task-control, cost, recovery, and action-state
  contracts through the same observable UI.

Dev-Log: DL-20260731-068
Date: 2026-07-31
Status: Complete
Change-ID: CL-20260731-067
Request-or-TODO: M16 public CI follow-up
Goal: Restore the cross-platform build gate without weakening the browser-only
  WASM entry-point boundary.
Assumptions: Shared client code must type-check natively even though executable
  browser startup exists only for js/wasm.
Decisions: Add a build-tagged no-op native main rather than exposing browser
  behavior on host targets or excluding the package from repository builds.
Files-or-schemas: Native client entry sentinel and change/development ledgers;
  no schema change.
Validation: `go build ./web/client` and `go test ./web/client -count=1` pass.
Failures-or-discarded-approaches: The M16 public platform jobs exposed the
  missing host main after their fast tests passed; changing the CI build scope
  was rejected because it would hide a real package-build defect.
Known-limitations: The replacement public workflow must confirm all supported
  operating systems; browser behavior remains covered by the separate WASM and
  Chromium gates.
Next-safe-step: Commit and push the focused correction, verify replacement CI,
  and continue M17 implementation.

Dev-Log: DL-20260731-067
Date: 2026-07-31
Status: Complete
Change-ID: CL-20260731-066
Request-or-TODO: M16-001 through M16-100 and M16-G01 through M16-G05; user
  requested reference-render fidelity, theming, i18n, live preview, full E2E,
  adversarial refinement, marked TODOs, and typed Go/GWC only.
Goal: Deliver the accessible local application shell and its browser evidence
  before streaming conversation and graph behavior add complexity.
Assumptions: The untracked design/reference directory is user-owned and
  read-only; generated scaffold assets remain disposable under `.artifacts`;
  server-confirmed identifiers are the only route state eligible for restore.
Decisions: Keep every authored frontend component in GoWebComponents v5; use
  system font stacks and local design tokens; compose explicit route and async
  state owners; persist only bounded layout values; lift graph selection into
  ephemeral root state; use one embedded loopback server; and gate acceptance
  on real browser behavior plus mounted render-boundary counters.
Files-or-schemas: Go client bootstrap/session entry points; frontend design,
  i18n, preferences, routes, primitives, shell, shortcut, session-client, and
  state packages; embedded frontend server and browser harness; coordinator
  route-access/bootstrap integration; local development command; M16 checklist
  and ledgers. No migration or durable schema change.
Validation: 247/247 Chromium checks pass across direct routes, wide/standard/
  compact/minimum layouts, keyboard traversal, visible focus, dialogs, rail
  and graph focus restoration, reduced visual viewport, selection persistence,
  accessibility, and zero external requests. Mounted Chromium evidence records
  cost `{T1 G1 M1 C1}->{T1 G1 M1 C2}`, chat
  `{T1 G1 M1 C2}->{T1 G1 M2 C2}`, and graph
  `{T1 G1 M2 C2}->{T1 G2 M2 C2}`. Full `test-all`, lint, generation,
  integration, security, artifact, native Go, and WASM gates pass.
Failures-or-discarded-approaches: Replaced a hook-owning accessible overlay
  invoked as a plain function after its closed/open transition crashed; rejected
  stale live bundles during visual review; fixed a first-paint preference race;
  and corrected a browser assertion that excluded the intentionally hidden
  selected graph node after switching tabs.
Known-limitations: M17 and later milestones will replace sample conversation,
  graph, metric, review, and assurance content with live task projections and
  command workflows. The current server remains a local development runtime.
Next-safe-step: Commit and publicly verify M16, then implement M17 thread rail,
  conversation timeline, composer, forecast, and plan-review behavior against
  the completed shell contracts.

Dev-Log: DL-20260731-066
Date: 2026-07-31
Status: Complete
Change-ID: CL-20260731-065
Request-or-TODO: M15 public CI follow-up
Goal: Restore the public cross-platform quality gate without weakening the
  repository's adapter-dependency policy
Assumptions: Storage errors are implementation details and transport handlers
  require only stable application-port classifications
Decisions: Keep transport-facing error sentinels at its application port and
  translate storage sentinels in the coordinator composition layer
Files-or-schemas: Task-control transport service, coordinator adapter, focused
  transport test; no schema change
Validation: `go test ./internal/transport ./internal/coordinator` and
  `go run ./cmd/codeflux-dev lint` pass
Failures-or-discarded-approaches: The original M15 implementation imported the
  storage adapter from the transport adapter; local validation had passed only
  because the tracked-file lint view was evaluated before that commit existed
Known-limitations: The superseded public M15 workflow remains red by design;
  the corrective commit must complete a fresh public workflow
Next-safe-step: Commit and push this focused correction, verify its public CI,
  then continue M16 shell integration

Dev-Log: DL-20260731-065
Date: 2026-07-31
Status: Complete
Change-ID: CL-20260731-064
Request-or-TODO: M15-001 through M15-045 and M15-G01 through M15-G03
Goal: Make interruption and process loss ordinary, explainable, and safe
  without losing work or repeating correctness-bearing actions
Assumptions: The coordinator owns durable truth; workers may disappear at any
  boundary; a persisted intent without a terminal outcome is ambiguous;
  recovery never silently changes the approved provider, model, policy, tools,
  repository, worktree, or plan.
Decisions: Canonicalize a closed checkpoint schema; derive runtime facts from
  authoritative repositories; preserve dirty state through an isolated Git
  index and private ref; atomically commit checkpoint events; close the action
  gate before pausing; wait for complete quiescence; transmit only exact durable
  checkpoint IDs; classify startup state before server bind; treat unresolved
  and post-checkpoint completed actions as replay blockers; require an explicit
  user decision before binary-safe patch export or recovery action.
Files-or-schemas: Migration 000015 and generated metadata; checkpoint,
  Git-preservation, recovery-observation, task-control, worker-control,
  transport, CLI, agent-boundary, application-composition, and test sources;
  M15 checklist evidence and change/development ledgers.
Validation: Real linked-worktree and binary export tests; SQLite atomicity,
  idempotency, close/reopen, and subprocess crash tests; six-trigger boundary
  matrix; unchanged, non-overlapping, conflicting, missing, policy/provider/
  tool-change, ambiguity, no-repeat, cancellation, shutdown, RPC, and CLI
  cases; go test ./..., lint, generation, migration, integration, security,
  git diff checks, and complete local test-all pass.
Failures-or-discarded-approaches: Rejected caller-authored checkpoint facts,
  hashes without patch preservation, branch-moving checkpoint commits,
  fabricated worker checkpoint IDs, optional product control registration,
  stale-revision checks before idempotent replay, provider-agnostic resume,
  outcome-unknown-only ambiguity, automatic external-effect replay, checkpoint
  sequence comparison against the pre-event snapshot, same-HEAD branch
  substitution, and timestamp values that changed after SQLite replay.
Known-limitations: Browser rendering and browser E2E begin at M16. Later task
  UI milestones consume the authenticated TaskService and structured recovery
  application services completed here.
Next-safe-step: Commit and publicly verify M15, then implement the M16
  GoWebComponents v5 shell, responsive design foundation, and browser harness
  without handwritten JavaScript, HTML, or CSS.

Dev-Log: DL-20260730-064
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-063
Request-or-TODO: M14-060 and M14 public-CI follow-up
Goal: Preserve exactly-once provider cancellation across macOS scheduler
  ordering during stream callback cancellation
Assumptions: Context cancellation may race attempt cleanup; provider Cancel is
  idempotent at the adapter boundary but the coordinator should issue it once
Decisions: Share one sync.Once-protected cancellation function between the
  watcher and deferred cleanup; cleanup synchronously observes a cancelled
  context before stopping the watcher.
Files-or-schemas: Provider execution coordinator and change/development ledgers
Validation: The public macOS ARM64 fast-test failure reported zero provider
  cancel calls. The focused cancellation regression passes 100 consecutive
  local executions; coordinator tests, lint, generation, and artifact checks
  pass.
Failures-or-discarded-approaches: The watcher previously selected between an
  already-ready context and an already-ready stop channel, so cleanup could
  win and suppress cancellation. Calling Cancel unconditionally in both paths
  was rejected because it permits duplicate calls.
Known-limitations: Public cross-platform CI must confirm the scheduler-specific
  regression on macOS ARM64.
Next-safe-step: Push the focused follow-up and require the replacement public
  CI run to pass while M15 implementation proceeds in independent files.

Dev-Log: DL-20260730-063
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-062
Request-or-TODO: M14-001 through M14-062 and M14-G01 through M14-G03
Goal: Compose persisted requirements, immutable plans, the fixed provider,
  mediated tools, hard budgets, bounded repair, and explicit review into the
  smallest reliable coding-agent state machine
Assumptions: M14 retains the frozen provider/model/effort policy; plans and
  validation are approved before effects; physical task-worktree paths and
  run identities are not known at intake; adaptive routing and multi-agent
  runtime topology remain deferred
Decisions: Derive requirement analysis from the stored original message;
  close step kinds and completion tools; bind plans to repository, context,
  policy, forecast, and budget revisions; project validation through one
  canonical logical-worktree contract while retaining raw commands only for
  user visibility; derive authority from policy semantics; require stronger
  profile labels to carry broader distinct required commands already approved
  in the plan; persist effect intent before execution and redacted outcomes
  afterward; checkpoint material edits; reconstruct completion only from
  durable step and validation evidence; require an explicit final decision
Files-or-schemas: Migration 000014 and generated schema/build metadata;
  executor validation-plan projection; agent loop, limits, tool schemas,
  repair, validation, and completion contracts; coordinator persistence and
  state-machine composition; requirement, plan, execution, validation,
  repair, completion, and review repositories; milestone tests and evidence
Validation: Executor, agent, coordinator, storage, and migration focused suites,
  go vet, go test ./..., git diff checks, deterministic fake-provider G01,
  real-SQLite triggers and recovery reconstruction, lint, generation, fast,
  integration, security, and complete local test-all pass. Frozen checksum
  7c304b402772e0fa688ee838a86c1ddda54008bfd48b76bec1f22b5ed9b468a0
  receives independent round-five ACCEPT.
Failures-or-discarded-approaches: Adversarial rounds rejected model-authored
  completion without durable proof, checkpoint ordering gaps, weak limit
  accounting, caller-forged requirement and plan fields, unsafe path-prefix
  scope, incomplete failure linkage, raw validation labels detached from
  executable requests, mutation misclassification, exact-only profile floors,
  label-only assurance upgrades, and plans that could not reach validation
  binding. Each accepted finding now has a deterministic invariant and
  regression. Caller-authored display authority, sensitive command arguments,
  silent test weakening, and auto-acceptance were rejected.
Known-limitations: Browser E2E remains intentionally unavailable until the
  M16 GWC harness. M15 must add production checkpoint persistence, cooperative
  interruption, divergence-aware resume, crash classification, and patch
  preservation before the frontend milestone may begin.
Next-safe-step: Commit and publicly verify M14, then complete M15's checkpoint,
  pause/cancel, resume, and recovery gates before implementing the mapped M16
  GoWebComponents v5 shell.

Dev-Log: DL-20260730-062
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-061
Request-or-TODO: M13-001 through M13-045 and M13-G01 through M13-G03
Goal: Make the prototype's fixed execution policy, forecast, and hard budget
  inspectable, reproducible, and enforceable before provider I/O
Assumptions: The plan's frozen baseline is OpenAI GPT-5.6 Sol at maximum
  reasoning; a provider revision creates a distinct evidence stratum; forecasts
  are advisory estimates; missing price or usage is unknown; external billed
  work may finish but cannot authorize subsequent work past the hard cap
Decisions: Freeze a versioned provider/model/effort and phase policy; retain
  transparent normalized forecast features and P50/P90 ranges; persist exact
  policy, forecast, budget snapshot, preflight, run binding, adjustment, and
  outcome evidence; share one optimistic budget revision across legacy and
  exact APIs; reserve the complete retry envelope before activating a planned
  provider request; reconcile reserved call slots to physical attempts; make
  the raw provider executor package-private; pause durably on pre-I/O budget
  exhaustion; keep learned routing advisory-only
Files-or-schemas: Migrations 000010 through 000013; policy and forecast
  packages; exact budget, preapproval adjustment, and execution-preflight
  repositories; coordinator preflight and budgeted-provider services; live
  provider lifecycle; generated schema metadata; milestone evidence
Validation: Deterministic baseline and drift tests, overflow-safe forecast
  validation, exact rational arithmetic, mixed-API concurrency, warning and
  hard boundaries, exact approval scope, settlement churn, reconciliation,
  retry-slot accounting, stale/missing preflight, dynamic presentation,
  terminal telemetry, cross-task rejection, and zero-I/O live budget tests
  pass. Generation, migration, lint, integration, security, complete local
  test-all, diff, and independent adversarial gates pass.
Failures-or-discarded-approaches: Early implementations accepted arbitrary
  baseline models, exposed an unbudgeted executor, started a live task before
  preflight, let two ledger APIs overspend together, bound raises too broadly,
  could abandon settlement after revision churn, ignored token-only overruns,
  counted retry maxima as actual calls, and presented stale budget values.
  Each defect now has a durable invariant and regression. Caller-controlled
  overage, unknown-price zero substitution, and adaptive model selection were
  rejected.
Known-limitations: Browser E2E begins with the M16 GWC harness. The real
  billable provider smoke remains explicit and must have immutable known
  pricing and sufficient budget. Public CI supplies cross-platform and Ubuntu
  race evidence after push.
Next-safe-step: Push M13, require public CI to pass, then implement M14's
  bounded requirement-plan-approve-edit-test-repair-review agent loop.

Dev-Log: DL-20260730-061
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-060
Request-or-TODO: M12-001 through M12-055 and M12-G01 through M12-G04
Goal: Normalize the initial provider set while preserving exact identity,
  usage, pricing, retry, cancellation, privacy, and recovery evidence
Assumptions: Provider capabilities and prices are revision-bound evidence;
  missing usage or pricing is unknown rather than zero; credentials remain in
  the operating-system store; a provider or model switch always requires
  explicit user authority
Decisions: Use one normalized request and ordered stream contract; implement
  OpenAI Responses, Anthropic Messages, and configurable OpenAI-compatible
  adapters; reject remote endpoints without approval; persist intent before
  provider I/O; seal pricing snapshots; reconcile standard and
  provider-specific categories; block retries after observable partial
  effects; compensate bounded pre-I/O, accounting, and terminal-write
  failures; pause after retry exhaustion; redact metadata before UI and again
  before persistence; keep live requests opt-in, cost-warned, content-free,
  OS-credential-only, and outside the repository database boundary
Files-or-schemas: Migration 000009; provider contracts, transports, adapters,
  retry, pricing, usage, and recovery; coordinator credential, attempt,
  execution, and recovery services; provider accounting and live-smoke
  repositories; cross-platform physical path resolution; run-live command and
  deterministic tests
Validation: Adapter mock and common-conformance suites, explicit and buffered
  cancellation, deadline classification, retry safety, exact rational and
  provider-specific accounting, sealed snapshot, recovery/finalization,
  Windows junction, no-network live-gate, migration, generation, lint,
  security, integration, and complete local test-all gates pass. Independent
  contract, runtime, and storage audits all return ACCEPT.
Failures-or-discarded-approaches: Cancellation initially raced buffered OpenAI
  output and mislabeled deadlines; accounting and terminal write failures
  could strand attribution; pricing components were appendable after snapshot
  creation; live finalization trusted caller outcomes; retry exhaustion
  failed to expose recovery choices; provider-specific usage was initially
  dropped. Each path now has a regression and durable invariant. Raw API keys,
  environment credential fallback, silent provider switching, unsealed or
  zero-valued unknown pricing, response-content logging, and repository-local
  live databases were rejected.
Known-limitations: Real billable provider smoke execution remains explicitly
  opt-in and is not run by CI. Browser E2E begins with the planned M16 GWC
  harness; public CI supplies cross-platform and Ubuntu race evidence after
  push.
Next-safe-step: Push M12, require public CI to pass, then implement M13 fixed
  routing, forecasts, reservations, and hard budget enforcement.

Dev-Log: DL-20260730-060
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-059
Request-or-TODO: M11-028 and M11-G01 public-CI follow-up
Goal: Preserve exact worker worktree verification across platform path aliases
Assumptions: A path spelling may differ after process launch while both paths
  still identify the same directory
Decisions: Keep the runtime working-directory contract unchanged and compare
  the child process current directory with the startup worktree through
  operating-system file identity in the subprocess test
Files-or-schemas: Worker process integration test only
Validation: The focused worker suite and full local tests pass. Public CI run
  30545302566 records exit status 12 from the prior direct string comparison
  on macOS ARM64 while the Ubuntu job passed.
Failures-or-discarded-approaches: Comparing os.Getwd text directly repeated the
  already-known macOS /var-to-/private alias defect. Weakening task worktree
  confinement was rejected; file identity retains the invariant.
Known-limitations: Cross-platform confirmation requires the next public CI
  matrix after this correction is pushed.
Next-safe-step: Push the correction, require all public CI jobs to pass, and
  continue the already-started M12 provider lanes.

Dev-Log: DL-20260730-059
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-058
Request-or-TODO: M11-001 through M11-048 and M11-G01 through M11-G03
Goal: Complete the coordinator and one-worker-per-task lifecycle with durable
  ownership, bounded concurrency, explicit recovery, and graceful shutdown
Assumptions: SQLite remains the only authoritative runtime store; the
  coordinator is single-instance per database; a coordinator restart cannot
  safely reattach to an old in-memory worker session; default process
  isolation is mediated workspace confinement rather than a perfect sandbox
Decisions: Use a file lock before migration and serving; bind only to literal
  loopback; generate per-launch browser and worker session secrets; persist
  lease ownership, process metadata, ordered heartbeats/reports, and the task
  queue; send worker startup only over an anonymous pipe; keep credentials out
  of worker startup and environment; classify expired/orphaned workers and
  invalid worktrees as recovery-required; separate worker and provider
  concurrency; bound reconnect, control queues, payloads, checkpoint grace,
  and forced process-tree termination
Files-or-schemas: Migration 000008; coordinator application,
  scheduler, supervisor, recovery, and gateway; worker protocol, client,
  process launcher, and platform process-tree controls; storage worker and
  queue repositories; focused crash, recovery, isolation, and shutdown tests
Validation: Focused worker, coordinator, storage, scheduler, and cancellation
  compensation suites pass. Migration, artifact, generation, lint, security,
  integration, and full local test-all gates pass. The deterministic
  development profile starts and self-checks on literal loopback. Public CI
  supplies the declared Ubuntu race and cross-platform process evidence.
Failures-or-discarded-approaches: A shell working-directory field was ignored,
  so repository commands now explicitly select the repository. Editing an
  uncommitted migration correctly triggered the generated-checksum guard.
  A failed worker launch initially reused its cancelled caller context for
  durable compensation; recovery now owns a separate bounded context.
  Unbounded shutdown waits, redirect-following authenticated clients, raw
  credentials, automatic uncertain recovery, and in-memory-only task queues
  were rejected.
Known-limitations: Default worker confinement is mediated rather than a
  perfect sandbox. Provider adapters remain M12 work, and the full GWC
  journey/browser harness remains gated on M16.
Next-safe-step: Verify the committed M11 state in public cross-platform and
  race CI, then implement M12 provider normalization and durable accounting.

Dev-Log: DL-20260730-058
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-057
Request-or-TODO: M11-015 through M11-019
Goal: Establish the minimum safe contract before launching or reconnecting a
  task worker
Assumptions: M11 continues with durable leases, live heartbeats, process
  launch, scheduling, and shutdown on top of this contract
Decisions: Version every startup/message envelope; require exact task and run
  identity, absolute non-root worktree, policy and tool-schema revisions,
  loopback-only coordinator URL, ordered sequence, and constant-time session
  token authentication. Keep credentials structurally absent. Define bounded
  container arguments and reconnect policy without implementing ambient retry.
Files-or-schemas: Worker protocol and focused tests; partial M11 checklist
Validation: Worker package tests pass
Failures-or-discarded-approaches: Remote coordinator endpoints, raw credential
  fields, unversioned payloads, multiple payload variants in one message, and
  unbounded reconnects were rejected
Known-limitations: Message transport, heartbeat ownership, process launch, and
  coordinator recovery remain in the unfinished portion of M11
Next-safe-step: Add durable worker leases and process metadata, then wire the
  authenticated heartbeat/control protocol into one worker per active task.

Dev-Log: DL-20260730-057
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-056
Request-or-TODO: M10-001 through M10-059 and M10-G01 through M10-G03
Goal: Let workers inspect, edit, and validate repositories through typed tools
  without ambient or unattributed authority
Assumptions: M10 owns policy and the subprocess boundary; M11 will wire these
  ports into authenticated worker lifecycle, and M16 will expose approval and
  progress state through the GWC interface
Decisions: Derive authority from typed side effects and exact command arrays,
  never from untrusted descriptions or claimed class. Bind grants to task,
  working directory, arguments, and effects; bind denials to capability and
  scope so changing tool names cannot regain authority. Require an attribution
  identifier for every executable task-scoped classification. Launch direct
  argument arrays in canonical task directories, pass only allowlisted
  non-secret environment variables, terminate process trees on timeout or
  cancellation, and redact before bounded persistence/display. Store custom
  commands as immutable typed templates after repository-source first-use
  approval. Define future plugins only as separately launched JSON-RPC/MCP
  subprocesses; do not load arbitrary code or build a marketplace.
Files-or-schemas: Executor tool protocol, policy, command runner, platform
  process-tree helpers, plugin boundary, and tests; storage repositories/tests
  for tool schemas, permission decisions, custom commands, command lifecycle,
  and redacted output; migration 000007; generated migration/build metadata;
  M10 checklist evidence and ledgers
Validation: Focused executor/storage tests, migration-check, generate-check,
  lint, security tests, and full local test-all pass. Tests cover exact grants,
  expiry, isolation, denial substitution, malicious display descriptions,
  credential stripping, output bounds/redaction, timeout/cancel trees,
  reviewed immutable templates, idempotency, and optimistic lifecycle writes.
Failures-or-discarded-approaches: Trusting the request's authority label,
  shell-concatenated commands, ambient coordinator environments, raw plugin
  secrets, repository-loaded code, mutable custom commands, and unbounded
  output were rejected. Effect classification was made order-independent so a
  reordered declaration cannot select weaker authority.
Known-limitations: Workspace confinement is mediation, not a perfect OS
  sandbox. Live coordinator/worker wiring belongs to M11, provider calls to
  M12, and browser presentation/E2E to M16-M20.
Next-safe-step: Implement M11 coordinator startup, single-instance ownership,
  authenticated worker protocol, heartbeats, scheduling, shutdown, and crash
  recovery without broadening the M10 authority boundary.

Dev-Log: DL-20260730-056
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-055
Request-or-TODO: M09-G01 through M09-G03 public-CI regression
Goal: Preserve strict worktree confinement without rejecting valid
  platform-level path aliases
Assumptions: Windows temporary directories may use 8.3 aliases and macOS
  exposes /var through /private; canonical identity must be established before
  deriving and persisting task paths
Decisions: Create and canonicalize the dedicated external worktree root at
  service construction, derive every task path from that canonical root, and
  compare already-resolved paths through filepath.Rel identity. Continue to
  reject actual symlink/reparse escapes and root overlap.
Files-or-schemas: Gitwork service root initialization, task path verification,
  and safe-file path identity checks
Validation: The focused Gitwork suite and lint pass on the local Windows host.
  Public CI run 30536861293 records the prior failures on Windows AMD64/ARM64
  and macOS ARM64 while Ubuntu and race jobs passed.
Failures-or-discarded-approaches: Direct string equality between a stored path
  and EvalSymlinks output treated operating-system aliases as movement.
  Weakening symlink validation was rejected; canonicalizing before derivation
  retains the confinement invariant.
Known-limitations: Cross-platform confirmation requires the next public CI
  matrix after this commit is pushed.
Next-safe-step: Push and require Windows, macOS, Ubuntu, and race jobs to pass,
  then accept the pending M10 authority milestone.

Dev-Log: DL-20260730-055
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-054
Request-or-TODO: M09-001 through M09-050 and M09-G01 through M09-G03
Goal: Make every supported source mutation isolated, conflict-aware,
  inspectable, reversible, and attributable to an exact task/base revision
Assumptions: M09 owns filesystem/Git mechanics and narrow persistence ports;
  M10 will apply the complete command authority policy, and M15 will extend
  checkpoint contents and crash classification without weakening these gates
Decisions: Place task worktrees under a typed repository/task convention
  outside the source repository and record exact base/expected HEAD in SQLite.
  Use cryptographic branch suffixes, fixed argument arrays, disabled hooks,
  optimistic binding revisions, strict no-symlink path resolution, exact
  existence/hash preconditions, rollback-on-event failure, and explicit
  approval for broad deletes. Build rename/binary-capable diffs with a copied
  disposable Git index so review never stages the user's real index. Pin
  checkpoint commits under private refs and require reviewed diff identity plus
  explicit author/mode for acceptance.
Files-or-schemas: Gitwork runner, worktree, file/edit, task-event, diff,
  checkpoint, acceptance implementations and real-Git tests; worktree binding
  and checkpoint storage operations/tests; migration 000006 and generated
  schema/build metadata; M09 evidence and ledgers
Validation: Focused Gitwork/storage tests and vet, migration-check,
  generate-check, lint, security tests, and test-all pass. Tests cover dirty
  primary repositories, branch collisions, cleanup after persistence failure,
  restart/missing/moved/external-commit detection, traversal and symlink
  escapes, UTF-8/binary/mode/newline handling, create/update/rename/delete,
  concurrent hashes, delete approval, transactional event rollback, exact
  diff classifications/attribution, real-index preservation, multi-checkpoint
  rollback, stale acceptance, all acceptance modes, author identity, and
  repository-hook suppression.
Failures-or-discarded-approaches: Raw XML-like worktree sidecars, shared source
  edits, shell command strings, automatic destructive cleanup, overwriting
  stale hashes, staging the user's actual Git index for review, deleting task
  branches on abandonment, and silently inventing commit authors were rejected.
  Ordinary Git diff omitted unstaged rename targets; an isolated copied index
  corrected rename/empty/binary review without mutating user state.
Known-limitations: The MVP provides mediated path confinement rather than a
  perfect OS sandbox. Symlink tests run where the OS grants link creation.
  M10 still owns general command policy, and M15 owns richer checkpoint schema
  and forced-termination recovery classification.
Next-safe-step: Implement M10 typed tools, executable permission policy,
  controlled subprocesses, approvals, cancellation, redacted output, and
  adversarial command-boundary gates.

Dev-Log: DL-20260730-054
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-053
Request-or-TODO: M08-029 through M08-053 and M08-G01 through M08-G04
Goal: Select only targeted, explainable repository context and preserve the
  exact redacted selection as revision-bound SQLite evidence
Assumptions: M08 exposes the complete all-Go context-card projection; M16 will
  render that model in the GWC timeline without introducing handwritten
  JavaScript, HTML, or CSS
Decisions: Use deterministic weighted ranking with explicit paths and symbols
  first, bounded direct-neighbor expansion, history only for admitted paths,
  separate file/byte/token caps, shared secret detection, and supporting-file
  hash invalidation. Treat instructions and suggested commands as untrusted and
  approval-gated. Serialize prompt excerpts as JSON under a fixed policy so
  repository text cannot terminate a boundary. Persist manifests, ordered
  excerpts, and exclusions transactionally in immutable strict tables.
Files-or-schemas: Workspace context selector/card/prompt boundary and tests;
  context-manifest storage contract/repository/tests; migration 000005 and
  generated migration/build-version metadata; M08 TODO evidence and ledgers
Validation: Focused workspace/storage tests and vet, migration-check,
  generate-check, lint, security tests, and test-all pass. Tests cover stable
  manifest identity/order, explicit and expanded selection, independent
  budgets, line/reason/trust metadata, stale-map invalidation, secret path and
  content exclusion, fake prompt-boundary escaping, approval gating, SQLite
  round trip/order/foreign keys/accounting/rollback/conflicts, and immutable
  item enforcement.
Failures-or-discarded-approaches: Raw XML-like prompt delimiters were rejected
  because repository content could supply a matching close marker; the final
  JSON envelope escapes structural characters. Whole-repository context,
  embeddings, dependency downloads, implicit custom-command approval, and a
  storage import from the workspace layer were excluded.
Known-limitations: Call and interface expansion inherits the conservative
  syntax/method-set limits of the M08 map. The card is a backend presentation
  model until the planned M16 GWC frontend consumes it.
Next-safe-step: Implement M09 Git worktree isolation, safe edit application,
  deterministic diff identity, checkpoints, and cleanup gates.

Dev-Log: DL-20260730-053
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-052
Request-or-TODO: M08-015 through M08-028
Goal: Produce a deterministic, revision-bound Go repository map without
  embedding the repository or granting authority to its content
Assumptions: The M08 runner is a narrow mediated read-only port; M10 later owns
  the complete typed worker command protocol and approval ledger
Decisions: Walk bounded non-vendor inputs without following symlinks, execute a
  fixed `go list -e -json ./...` per module with GOPROXY/GOSUMDB disabled, and
  parse local Go files with standard AST tooling. Preserve invalid-file hashes
  and bounded warnings, use sorted slices and a canonical digest for map
  revision, infer only cheap syntactic call/method-set relationships, and mark
  repository command/instruction sources untrusted and approval-required.
Files-or-schemas: Network-disabled workspace command runner, Go repository map
  implementation/tests, and M08 evidence; no schema change
Validation: A committed multi-module fixture proves module Go versions,
  packages/imports/tests/targets, exported and internal symbols, definitions
  and references, caller/callee edges, interface implementations, generated
  files, build constraints, command discovery, instruction classification, and
  deterministic map revision. An invalid package records warnings without
  hiding valid packages; absent modules and network-dependent imports fail
  safely. Workspace tests, vet, lint, and the full local suite pass.
Failures-or-discarded-approaches: Whole-repository embeddings and dependency
  downloads were excluded. Human-readable go-list output, shell commands,
  vendor traversal, and pretending ambiguous selector calls are resolved were
  rejected. Invalid files remain hashed but do not contribute false symbols.
Known-limitations: Caller/callee and implementation relationships are
  conservative syntax/method-set candidates rather than whole-program
  pointer-analysis proof. Dependency packages outside the repository are not
  indexed.
Next-safe-step: Build deterministic, budgeted context selection with explicit
  path/symbol priority, explanations, secret exclusion, and SQLite manifests.

Dev-Log: DL-20260730-052
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-051
Request-or-TODO: M06-018, M06-019, and M06-G02
Goal: Eliminate the remaining Linux race at every native conformance-client
  teardown without suppressing race coverage or forking the pinned bridge
Assumptions: Browser WASM uses the bridge's separate JavaScript connection
  adapter; the reported defect occurs only in native test cleanup when
  grpc.ClientConn.Close calls SetWriteDeadline concurrently with the native
  adapter's final Gorilla WebSocket write
Decisions: Track accepted native test connections, close the peer transport
  first, wait until the client leaves Ready/Connecting, and only then release
  the grpc.ClientConn. Keep the race suite enabled and retain v1.1.1 because no
  newer upstream release exists.
Files-or-schemas: Native transport-spike test lifecycle and corrected M06
  evidence/ledgers; no production or schema change
Validation: Twenty consecutive focused package runs and full repository lint
  pass locally. The next public Linux AMD64 race job supplies authoritative
  confirmation.
Failures-or-discarded-approaches: Receiving EOF fixed the reconnect test but CI
  reproduced the same dependency race after a maximum-payload response,
  disproving CL-20260730-049's narrow explanation. Excluding the package from
  race tests, sleeping, adding a custom bridge, vendoring an unreviewed fork,
  or modifying the separate upstream repository were rejected.
Known-limitations: The pinned upstream native adapter still uses a deadline
  mutex separate from its writer mutex. Codeflux's native conformance lifecycle
  avoids that unsupported concurrent call pattern; the browser WASM adapter is
  structurally separate.
Next-safe-step: Push and confirm the public race matrix, then complete the
  already-started deterministic Go repository-map slice.

Dev-Log: DL-20260730-051
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-050
Request-or-TODO: M08-001 through M08-014
Goal: Open and inspect a Git repository read-only, producing a deterministic
  snapshot bound to stable identity and an exact revision
Assumptions: M08 uses a small bounded command-runner port before M10 adds the
  full worker tool protocol; Git and filesystem reads are fixed by Codeflux and
  never constructed from repository instructions
Decisions: Canonicalize both selection and Git-reported root and require an
  ancestor relationship. Derive path-independent Git identity from the root
  commit, retain the existing SQLite repository ID/path separation, sanitize
  remote user information, parse porcelain-v2 status, inspect operation
  markers, walk without following symlinks, and scan only bounded tracked-file
  prefixes for LFS pointers without fetching.
Files-or-schemas: Workspace discovery implementation/tests and M08 evidence;
  no schema change
Validation: Temporary repositories prove nested selection, identity across a
  path move, exact HEAD binding, branches/remotes/status, detached HEAD,
  conflicts, merge/rebase/cherry-pick/bisect markers, submodules, nested
  repositories, LFS pointers, ignored/untracked content, unsafe root rejection,
  invalid selections, empty history, cancellation, and output bounds. Workspace
  tests and repository lint pass.
Failures-or-discarded-approaches: Treating the canonical path as repository
  identity was rejected because paths move. Parsing human-readable Git status
  was rejected in favor of NUL-delimited porcelain v2. Repository-provided
  commands and automatic LFS/submodule network access were excluded.
Known-limitations: The prototype records submodules as unsupported and does not
  inspect their content. Root-commit identity changes after destructive history
  rewriting; the durable Codeflux repository ID remains authoritative.
Next-safe-step: Build the M08 deterministic Go repository map from the frozen
  snapshot revision through the bounded command port.

Dev-Log: DL-20260730-050
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-049
Request-or-TODO: M06-018, M06-019, M06-024, and M06-G02
Goal: Repair the two cross-platform failures reported by the authoritative
  public CI matrix before proceeding with M08
Assumptions: GoGRPCBridge v1.1.1 remains the pinned decision; its client
  connection is safe to close after the resumed RPC has delivered its terminal
  status and the service reports no active streams
Decisions: Reject backslashes and drive-qualified paths lexically rather than
  delegating all absolute-path recognition to host-specific filepath rules.
  Receive the resumed stream EOF and wait for service quiescence before test
  teardown instead of replacing or patching the selected bridge.
Files-or-schemas: Retained transport-spike validation and reconnect test plus
  M06 evidence and ledgers; no schema change
Validation: The focused transport-spike package and full repository lint pass
  on Windows ARM64. Public CI supplies the required Linux AMD64 race and
  macOS/Linux host-path checks.
Failures-or-discarded-approaches: Windows-only local testing had accepted the
  false assumption that filepath.IsAbs recognizes Windows drive paths on Unix.
  The reconnect test also stopped after the final payload rather than receiving
  EOF, allowing deferred connection close to race the pinned bridge's writer.
  A bridge fork was rejected because orderly RPC completion removes the
  teardown race without expanding transport surface.
Known-limitations: Local Windows ARM64 still cannot execute the Go race
  detector; the correction is not considered fully confirmed until public CI
  passes.
Next-safe-step: Push the focused correction, verify the public matrix, then
  resume the uncommitted M08 repository-discovery slice.

Dev-Log: DL-20260730-049
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-048
Request-or-TODO: M07-061 through M07-090 and M07-G01 through M07-G04
Goal: Freeze the complete application-service contract and prove every
  chronological backend flow against deterministic fakes before repository
  intelligence begins
Assumptions: M07 defines contracts and executable acceptance models rather than
  prematurely implementing M08 and later adapters; product RPC messages and
  generated bindings remain the authoritative transport vocabulary
Decisions: Represent every RPC and backend function as typed Go catalog data.
  Record request/result types, authority, idempotency, duplicate behavior,
  expected revision, transaction, repositories, events, external-effect
  intent/outcome/ambiguity/retry, cancellation, and safe errors explicitly.
  Use generated Bufconn clients for the requirement journey and small
  deterministic fixtures for the remaining chronological acceptance matrices.
Files-or-schemas: Coordinator contract catalog and tests, deterministic backend
  flow fixtures, transport dependency correction, and M07 completion evidence;
  no protobuf or SQLite schema change
Validation: All 37 product methods match descriptors and have one thin-handler
  mapping. Every planned backend category and representative function validates
  complete mutation/effect semantics. Startup/recovery, repository safety,
  submit/forecast/plan/revise/approve/start, tool decisions, durable task
  controls, review decisions, replay, crash classification, retrieval, and atom
  admission tests pass. Bootstrap, deterministic generation, migration check,
  lint, fast, integration, security, all-scope, and artifact checks pass.
Failures-or-discarded-approaches: The first catalog draft left request/result
  message names and effect outcome/ambiguity/retry behavior implicit; the audit
  rejected that and made each field descriptor-checked. The first replay
  fixture used an incorrect retention boundary and was corrected. Full lint
  exposed an earlier transport import of storage sentinels; direct
  adapter-to-adapter mapping was removed so application errors cross the inward
  contract instead.
Known-limitations: These tests prove the M07 contract and deterministic fake
  flows, not production implementations of repository discovery, providers,
  workers, tools, review, graphs, or memory. Browser E2E remains scheduled for
  the implemented product UI milestone, and the race suite remains authoritative
  in Ubuntu AMD64 CI because the local host is Windows ARM64.
Next-safe-step: Begin M08 at repository opening and safe root discovery, using
  the cataloged WorkspaceService boundary and plan section 27 repository rules.

Dev-Log: DL-20260730-048
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-047
Request-or-TODO: M07-051 through M07-060
Goal: Put one testable security, validation, observability, cancellation, and
  safe-failure boundary around every generated product RPC
Assumptions: The production grpc.Server remains in-process behind the M06
  bridge; browser cookie authentication occurs before upgrade and its
  server-generated request ID may be trusted only when explicitly configured;
  native callers carry the local session token as metadata
Decisions: Use constant-time token comparison, protobuf-reflection validation,
  typed application errors with ApiErrorDetail, payload-free structured logs,
  validated/generated correlation IDs, and a graceful-stop deadline fallback.
  Keep business policy out of interceptors and handlers.
Files-or-schemas: Product boundary implementation and in-process tests plus
  M07 evidence/ledgers; no protobuf or SQLite change
Validation: Every one of 37 methods passed through Bufconn authentication and
  validation, and generated clients completed the synthetic workspace,
  thread/message, task, graph, review/acceptance, and session-stream journey.
  Malformed controls, identity/bounds, unauthorized sessions, stale revisions,
  duplicates, deadlines, safe details, bridge attestation, correlation/log
  shape, and graceful shutdown have focused passing tests. Repository lint
  passes.
Failures-or-discarded-approaches: Treating same-origin as authentication was
  rejected. Forwarding the cookie into gRPC metadata was unavailable and would
  duplicate secret exposure; instead the app-owned upgrade validates the
  cookie and the private in-process gRPC boundary may explicitly trust only the
  bridge-generated request attestation. Dynamic protobuf test messages first
  exposed an unsafe concrete type assertion; validation now reads reflection
  values and supports generated and dynamic clients equally.
Known-limitations: The in-process fake proves the transport journey but does
  not yet supply application-domain semantics; the function catalog and
  chronological deterministic flow model begin at M07-061.
Next-safe-step: Add the machine-reviewable method/application catalog and prove
  every authority, revision, transaction, event, external-effect,
  cancellation, and typed-error contract.

Dev-Log: DL-20260730-047
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-046
Request-or-TODO: M07-001 through M07-050
Goal: Freeze the complete production transport vocabulary before interceptor,
  handler, application-catalog, or chronological-flow implementation
Assumptions: The selected M06 WebSocket bridge uses ordinary generated Go gRPC
  bindings in WASM; M07 contracts describe domain views and commands rather
  than database rows or future subsystem implementations
Decisions: Keep one versioned Go/protobuf package, seven logical services, and
  one server stream. Use a shared mutation control for idempotency and optional
  optimistic revision, opaque cursor pagination, exact minor-unit money,
  explicit redacted text, standard protobuf time values, caller-bounded graph
  slices, safe relative editor paths, and provider credential references only.
Files-or-schemas: Conventions, resources, and product API protobuf files;
  generated message/gRPC bindings; descriptor boundary tests; M07 completion
  and evidence ledger entries; no database schema
Validation: All 37 methods and generated client constructors are present;
  every mutation has standard control; only SubscribeSession streams; product
  fields expose no SQLite, SQL, raw credential, password, or API-key value.
  Buf lint/breaking, generation/check, generated compilation, and repository
  lint pass.
Failures-or-discarded-approaches: Reusing storage-shaped or generic CRUD
  messages was rejected. Raw provider credentials were excluded in favor of
  opaque credential references. Multiple chat/graph/cost streams were rejected
  in favor of the existing ordered SessionEvent envelope.
Known-limitations: Contracts are generated and validated but production
  interceptors, thin handlers, application delegation, error mapping, and
  deterministic fake flows begin at M07-051.
Next-safe-step: Implement the authenticated validation/error/logging server
  boundary and prove every method through in-process generated clients.

Dev-Log: DL-20260730-046
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-045
Request-or-TODO: M06-001 through M06-038 and M06-G01 through M06-G05
Goal: Remove frontend-framework, browser-transport, reconnect, and SVG load
  uncertainty before production gRPC and UI work
Assumptions: Spike RPCs are retained conformance fixtures rather than M07
  product contracts; browser authorization may use a host cookie because the
  WebSocket API cannot add a custom header; fixed-height thread rows are
  sufficient for the bounded virtualization experiment
Decisions: Pin GWC v5.0.1 and GoGRPCBridge v1.1.1. Use the bridge's
  same-origin WebSocket tunnel into an in-process grpc.Server and reject Envoy,
  gRPC-Web fallback, and a new custom bridge after all gates pass. Author the
  client entirely in Go/GWC and generate the shell/runtime/WASM only beneath
  `.artifacts`. Retain the fixture and security/load tests. Use an ignored
  private launch-secret file only for intentional restart testing.
Files-or-schemas: GWC browser client, transport-spike protobuf and generated
  unary/server-streaming bindings, embedded bridge/service/server and tests,
  spike server and launch-secret tests, cross-platform build/run helpers,
  pinned generator/bootstrap checks, module dependencies, plan/TODO evidence,
  and agent command documentation; no SQLite migration
Validation: The typed bridge delivered 10,000 ordered events and measured
  575,700 tunnel bytes for 369,873 protobuf bytes (1.556 ratio). Browser
  evidence covered cancellation, sequence 6,657 coordinator interruption and
  exact resume to 10,000, active-stream refresh, Back/Forward, 14 rendered rows
  from 10,000, isolated chat/graph rerenders, editor approval gating, clipboard
  denial, semantic names, visible focus, and arrow navigation. The 300-node
  view used 717 DOM elements at p95 18.5-19.2 ms; 1,200 nodes used 2,517 at p95
  19.0 ms, so the measured degradation threshold remained above 1,200.
  Bootstrap, WASM build, generation-check, migration-check, lint,
  fast/integration/security/all tests, benchmark, and artifact-check pass.
Failures-or-discarded-approaches: The first real browser load exposed a CSP
  omission for Go WASM and rejection of a Secure `__Host-` cookie over
  loopback HTTP; adding `wasm-unsafe-eval` and using a host-scoped HttpOnly
  SameSite=Strict cookie fixed those failures. Native browser navigation waits
  block on SPA history, so Back/Forward were asserted with bounded observation
  of the visible URL/DOM. Initial lint rejected nonconforming RPC message names
  and deprecated `grpc.WithBlock`; the schema now uses service-prefixed
  request/response names and nondeprecated dialing. The conditional gRPC-Web
  versus custom-bridge comparison was discarded because the selected embedded
  bridge passed.
Known-limitations: Browser automation did not grant clipboard-write
  permission, fixed-height virtualization is the proven mode, the development
  GWC shell requires `wasm-unsafe-eval`, debug WASM size is not a production
  size result, and Windows ARM64 cannot run Go race instrumentation; Ubuntu
  AMD64 CI remains the race authority.
Next-safe-step: Define M07's small domain-oriented production gRPC contracts
  on the selected transport without expanding the retained spike service.

Dev-Log: DL-20260730-045
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-044
Request-or-TODO: M05-016 through M05-038 and M05-G01 through M05-G03
Goal: Complete the durable ordered session stream, exact reconnect behavior,
  and command idempotency required before the framework/transport spike
Assumptions: One reconnectable session belongs to one thread; SQLite remains
  the full durable history even when an individual live subscriber is too slow;
  schema version 1 snapshots and payloads are the only accepted versions
Decisions: Allocate sequence by advancing the session row inside the event
  transaction. Serialize publication against the replay boundary. Coalesce
  only message deltas and same-execution tool progress, preserving arrival
  order; disconnect rather than silently lose material facts. Store immutable
  snapshots and command results, and reduce task state with strict sequence,
  revision, and from-state checks.
Files-or-schemas: Session event JSON codec, event hub/subscription/metrics,
  snapshot/reducer, schema 4, session journal/replay/command repositories,
  storage decision record, milestone evidence, generated migration/build
  metadata, and focused concurrency/restart/property tests
Validation: Focused event and storage suites pass. They prove rollback-safe
  allocation, 64-way concurrent contiguous sequences, serialized replay/live
  join, post-commit observation, bounded pressure behavior, cancellation
  cleanup, every reconnect boundary, interleaved facts, restart snapshots, and
  one effect across 16 concurrent command retries.
Failures-or-discarded-approaches: Windows ARM64 cannot execute Go's race
  instrumentation, so local `go test -race` was rejected by the toolchain;
  the repository's Ubuntu race job remains the authoritative race gate. Queue
  replacement was changed from in-place overwrite to remove-and-append so a
  newer coalesced event cannot appear before an older material event.
Known-limitations: Browser transport and GoWebComponents do not exist yet;
  snapshot construction is explicit and durable compaction scheduling belongs
  to later lifecycle work.
Next-safe-step: Execute the M06 GoWebComponents v5 and typed transport spike,
  record measured compatibility/security/rendering results, and select the
  retained browser transport before implementing product UI.

Dev-Log: DL-20260730-044
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-043
Request-or-TODO: M05-001 through M05-015
Goal: Freeze the complete typed event vocabulary before implementing journal
  persistence, publication, subscription, or replay
Assumptions: A session is the ordered reconnect boundary and may include thread
  events before a task exists; graph encoded changes are structural,
  schema-owned payloads; payload schema version 1 is the only accepted version
Decisions: Add a distinct `ses_` stable identity. Use one protobuf oneof and a
  domain one-of with exact kind matching. Classify only message deltas and tool
  progress as ephemeral/coalescible; make authority, correctness, recovery, and
  error facts material. Keep all user-presentable strings explicitly redacted.
Files-or-schemas: session protobuf/generated Go, stable identity protobuf/domain/
  transport changes, session event domain contract/tests, generated registry,
  Buf local module-pinned plugin configuration/bootstrap checks, docs/plan.md,
  TODOS.md, CHANGELOG, DEVLOG; no SQLite migration
Validation: All 20 initial event kinds passed typed envelope validation and UI
  safety. Tests rejected unversioned, mismatched, and multi-payload envelopes
  and verified non-droppable classifications. Session identity round-tripped
  through domain and protobuf conversion. Generation and isolated
  generation-check passed with the module-pinned protoc-gen-go dependency.
Failures-or-discarded-approaches: The Buf remote plugin registry repeatedly
  returned resource-exhausted during generation. Replaced it with Buf's local
  command-array support invoking the already pinned Go module dependency,
  removing registry rate limits without adding a global tool. The first full
  lint run also correctly rejected a token-shaped redaction fixture; the test
  now assembles that synthetic value at runtime so tracked source contains no
  credential-shaped literal.
Known-limitations: No SessionEvent SQLite table, append/publish service,
  subscriptions, replay join, or command-result idempotency exists yet.
Next-safe-step: Add schema 4 and the atomic append API, then publish only the
  committed returned event into bounded session/task subscribers.

Dev-Log: DL-20260730-043
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-042
Request-or-TODO: M04-021 through M04-032 and M04-G01 through M04-G03
Goal: Close M04 with one inspectable redaction mechanism and end-to-end secret
  absence across the currently implemented task boundaries
Assumptions: Bounded chunk accumulation is sufficient before the later live
  event-stream milestone; arbitrary entropy is not evidence of a credential
Decisions: Use a constant marker, longest-first exact matching, provider/header/
  bearer/PEM patterns, and recursive sensitive-field handling. Bound raw input
  with a maximum-secret-length guard and bound output again. Keep structured
  truncation valid JSON. Disable entropy matching until a measured
  false-positive study authorizes it.
Files-or-schemas: internal/redact pipeline, corpus/stream/structured/gate tests,
  configuration permission-escalation test, docs/plan.md, TODOS.md, CHANGELOG,
  DEVLOG; no schema change
Validation: Exact and patterned material was removed at all four boundaries.
  Tests split three fixture classes at every byte boundary and exercised quoted,
  spaced, header, assignment, private-key, provider-exception, and HTTP forms.
  Structured fields were redacted before serialization and oversize output
  remained valid JSON. A real-SQLite mock task persisted redacted prompt/event
  data and retained the known credential in no output, database, WAL, or
  shared-memory page. Repository permission/command self-grants were rejected.
Failures-or-discarded-approaches: Replaced post-limit raw JSON slicing with a
  small valid truncation object. Entropy detection was not implemented because
  its required false-positive acceptability evidence does not exist.
Known-limitations: Live incremental emission is deferred to the M05/M08 session
  stream; the current stream redactor returns one bounded final result.
Next-safe-step: Begin M05 with the typed SessionEvent envelope, immutable/
  droppable classification, and atomic append-before-publish contract.

Dev-Log: DL-20260730-042
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-041
Request-or-TODO: M04-010 through M04-020
Goal: Make provider credential handling possible on Windows without allowing
  raw values into formatting, serialization, SQLite, or worker processes
Assumptions: Windows is the active development platform; macOS Keychain and
  Linux Secret Service remain honest unavailable backends in this milestone;
  explicit environment fallback is read-only
Decisions: Represent secrets as opaque copied bytes with callback-only use and
  explicit clearing. Implement generic Credential Manager records through the
  native Windows API behind a testable adapter. Persist only
  `os://service/account`; scrub child environments by explicit names and common
  provider-secret suffixes. Keep non-Windows native stores unavailable instead
  of selecting environment fallback automatically.
Files-or-schemas: internal/credentials, internal/worker environment boundary,
  migration 000003 and generated schema metadata, provider credential-reference
  repository/tests, CLI doctor, docs/plan.md, docs/storage.md, TODOS.md,
  CHANGELOG, DEVLOG
Validation: Unit tests passed secret formatting/serialization refusal,
  temporary-copy clearing, destroy behavior, explicit environment fallback,
  and Windows create/update/retrieve/test/delete through a deterministic fake.
  A real child process received no API-key/access-token fixtures. A real SQLite
  database retained the opaque reference but none of the known secret material
  in database or sidecar pages. Linux AMD64 and macOS ARM64 credential test
  binaries compiled beneath `.artifacts/bin`.
Failures-or-discarded-approaches: Native keyring integration tests do not write
  disposable fixtures into the user's real Credential Manager; the Windows API
  boundary is injected and tested without mutating user credentials. Removed
  any implicit non-Windows or environment fallback.
Known-limitations: Native macOS and Linux stores remain unavailable by explicit
  product decision. The redaction pipeline and full mock-task secret gate are
  still incomplete.
Next-safe-step: Implement one streaming-safe redaction pipeline used before
  persistence, prompt, UI, and diagnostic boundaries, then run the three M04
  security gates.

Dev-Log: DL-20260730-041
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-040
Request-or-TODO: M04-007 and M04-008
Goal: Make configuration use attributable and replayable without persisting
  provider secrets or private task content
Assumptions: Existing tasks cannot truthfully recover a pre-migration effective
  configuration, so revision zero represents a legacy/default binding rather
  than fabricated settings
Decisions: Store immutable globally numbered user and approved-repository
  revisions; bind each repository-created task through a foreign-key table;
  require run snapshots to match that binding through a database trigger;
  content-hash exact JSON and recursively reject secret-shaped field names at
  the storage boundary.
Files-or-schemas: migration 000002, generated migration catalog and schema
  version, storage configuration repository/contracts/tests, task creation
  binding, docs/storage.md, TODOS.md, CHANGELOG, DEVLOG
Validation: Real-SQLite tests passed revision allocation, repository approval
  lineage, identical retry, changed retry conflict, task binding, effective run
  record/read/retry, wrong-binding constraint, and nested secret-field
  rejection. The migration suite passed empty and every-version upgrades
  through schema 2.
Failures-or-discarded-approaches: Used a separate task binding table instead of
  adding a non-null reference column with a synthetic default, because SQLite
  restricts adding referenced columns with non-null defaults under foreign-key
  enforcement. The explicit legacy revision avoids inventing historical data.
Known-limitations: Run creation is still direct storage setup until its later
  application-service milestone; native credential stores and the shared
  redaction pipeline remain incomplete.
Next-safe-step: Define the credential-store interface and opaque secret value,
  implement the Windows native backend with explicit environment fallback, and
  prove workers and SQLite never receive raw credentials.

Dev-Log: DL-20260730-040
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-039
Request-or-TODO: M04-001 through M04-006 and M04-009
Goal: Establish one inspectable, non-secret configuration contract before
  adding persistent settings or native credential stores
Assumptions: Environment values belong to the ephemeral user layer; repository
  configuration is untrusted even when it contains only ordinary settings;
  an absent provider endpoint means no provider is configured
Decisions: Fix precedence as task over approved repository over user over
  defaults and retain a source for each field. Require approval for every
  non-empty repository overlay. Allow only five named non-secret environment
  settings. Permit HTTP only for loopback providers. Bound imports to 64 KiB
  and reject every unknown field instead of attempting forward compatibility
  across security-sensitive configuration.
Files-or-schemas: internal/config package and tests, docs/plan.md, TODOS.md,
  CHANGELOG, DEVLOG; no schema change
Validation: Configuration tests passed fixed precedence and source attribution,
  mandatory repository approval, endpoint/userinfo/TLS rules, non-negative
  exact budgets, timeout bounds, non-root absolute worktree paths, policy
  validation, environment allowlisting, strict round-trip import/export,
  oversize rejection, trailing-data rejection, and secret-shaped unknown-field
  rejection. Package vet passed.
Failures-or-discarded-approaches: Kept credentials entirely outside the
  settings structs rather than representing them as redactable strings.
  Unknown imported fields are rejected because silently retaining or ignoring
  a future permission-bearing key would weaken the boundary.
Known-limitations: SQLite settings revisions, task/run bindings, native
  credential stores, and redaction are the next incomplete M04 slices.
Next-safe-step: Add immutable settings revisions and effective run snapshots in
  SQLite, bind tasks to a settings revision, then implement credential-store
  interfaces without exposing secret values to formatting or workers.

Dev-Log: DL-20260730-039
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-038
Request-or-TODO: M03-G01 through M03-G04
Goal: Close the SQLite milestone with executable evidence for crash durability,
  event replay equivalence, credential absence, and migration recovery
Assumptions: Task creation is the implicit draft-state fact for the initial
  schema; ordinary non-transition task events do not affect task-state replay
Decisions: Replay ordered immutable events rather than reading the aggregate;
  require contiguous sequences and domain-valid source/target transitions;
  classify any mismatch as corruption. Exercise crash recovery through the
  current test executable as a child so the operating system terminates a real
  process while both aggregate and event writes are open in one transaction.
Files-or-schemas: internal/storage repository contract, replay implementation,
  durability/replay gate tests, docs/storage.md, TODOS.md, CHANGELOG, DEVLOG; no
  schema change
Validation: Focused replay and forced-termination tests passed. The killed
  transaction left neither its task update nor event, while the prior committed
  transition survived; reopen passed integrity and foreign-key checks and
  replay matched aggregate state/revision. Existing schema introspection,
  path-free doctor, migration, online backup/restore, interrupted restoration,
  and unsupported-newer-schema tests passed in the complete local suite.
  `go run ./cmd/codeflux-dev test-all` passed lint, generation-drift, fast,
  integration, and security suites.
Failures-or-discarded-approaches: Added an affected-row assertion inside the
  killed transaction so the crash fixture cannot pass without performing the
  intended aggregate write. Replay rejects gaps instead of silently sorting
  around missing correctness-bearing history.
Known-limitations: Browser E2E remains gated on M16. Local race execution is
  unsupported on Windows ARM64; Ubuntu CI remains the authoritative race run.
Next-safe-step: Begin M04 from its configuration precedence and typed settings
  contracts, then add native credential-store boundaries and the shared
  redaction pipeline without persisting any provider secret.

Dev-Log: DL-20260730-038
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-037
Request-or-TODO: M03-061 through M03-069
Goal: Complete the M03 operator-facing database lifecycle without leaking
  storage paths, content, credentials, or raw SQLite failures
Assumptions: Verbose redacted command output needs a fixed local bound before
  later settings UI exists; projects and threads preserve identity through a
  tombstone phase while learned artifact erasure removes owned lineage
Decisions: Retain only 30 days and the newest 8 MiB per command; use SQLite's
  online backup API for explicit snapshots and full integrity_check for
  operator checks; checkpoint WAL in truncate mode on close; require all
  future graph/vector ownership to use foreign keys and pass an orphan check;
  report diagnostic counts and versions but no selected filesystem paths
Files-or-schemas: cmd/codeflux maintenance commands and tests,
  internal/storage maintenance lifecycle and tests, docs/storage.md, TODOS.md,
  CHANGELOG, DEVLOG; no schema change
Validation: Real-SQLite tests passed for health/size/version diagnostics, full
  integrity, truncate checkpoint, foreign-key lineage, age expiry, newest-byte
  retention, explicit deletion policy, backup creation, and backup integrity.
  `go run ./cmd/codeflux-dev test-all` passed lint, generation-drift, fast,
  integration, and security suites.
Failures-or-discarded-approaches: Removed resolved Git executable paths from
  doctor output after security review showed the path was unnecessary
  diagnostic data. Deferred close calls were replaced with explicit checked
  shutdowns so checkpoint failure cannot be reported as command success.
Known-limitations: Browser tests remain unavailable until M16. Local race
  execution is unsupported on Windows ARM64 and remains assigned to Ubuntu CI.
  The M03 forced-termination, replay-equivalence, credential-absence, and
  migration/recovery gates are not yet marked complete.
Next-safe-step: Add deterministic forced-process-termination and task-event
  replay tests, prove schema and diagnostic credential absence, then close all
  four M03 gates before beginning M04.

Dev-Log: DL-20260730-037
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-036
Request-or-TODO: M03-049 through M03-054 and M03-056 through M03-058
Goal: Complete the M03 domain repository surface with atomic replay facts,
  explicit authority, exact budget accounting, and evidence lineage
Assumptions: Task transitions always emit one deterministic event; idempotent
  retries reuse identity and content; event order is per task; callers release
  a declared reservation amount when posting actual cost
Decisions: Bind transition update and event insert in one immediate transaction;
  allocate max sequence under the single-writer lock; compare retry identity,
  content, and lineage; use optimistic revisions for approvals and budgets;
  prevent integer overflow before SQL; validate run/task and
  validation/task relationships before commit
Files-or-schemas: internal/storage task, approval/budget, checkpoint,
  validation/evidence repositories, contracts and real-database tests,
  docs/storage.md, TODOS.md, CHANGELOG, DEVLOG; no schema change
Validation: Task creation and typed read pass, one draft-to-forecasting
  transition returns the same task/event on retry and rejects changed or stale
  claims. Twenty concurrent event appends produce exactly sequences 1 through
  20. Approval creation retries exactly and resolution advances one revision.
  Budget reserve/post produces reserved 100, actual 250, tokens 10 and rejects
  stale and over-cap changes. Checkpoint retry, validation, and evidence
  persistence pass. The complete storage suite passes locally.
Failures-or-discarded-approaches: Replaced collapsed optional-run query errors
  with a shared lineage verifier that distinguishes missing constraint,
  database failure, and cross-task mismatch. Local `go test -race` is
  unavailable on Windows ARM64; the declared Ubuntu hosted race job remains the
  required race verification.
Known-limitations: Diagnostics, maintenance, process-termination recovery gate,
  replay projection, and deletion/retention operations remain in M03
Next-safe-step: Implement doctor database checks, explicit backup and integrity
  operations, shutdown checkpointing, retention and deletion semantics, size
  and version diagnostics, then exercise the M03 forced-termination and replay
  gates

Dev-Log: DL-20260730-036
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-035
Request-or-TODO: M03-045 through M03-048
Goal: Establish domain-shaped persistence and deterministic conversation
  primitives without leaking generic SQL into application packages
Assumptions: Callers supply already parsed stable IDs and canonical repository
  locations; message retries reuse both identity and idempotency key; thread
  sort order is newest update then stable typed identity
Decisions: Define ProjectOperations and ConversationOperations; scan SQL
  directly into typed IDs; verify repository/project lineage during thread
  insert; paginate with an exclusive composite cursor; allocate sequence and
  compare idempotent message content inside one immediate transaction
Files-or-schemas: internal/storage repository contracts, project, repository,
  thread, message implementations and real-database tests, docs/storage.md,
  TODOS.md, CHANGELOG, DEVLOG; no schema change
Validation: Typed project and repository records round-trip; duplicates map to
  conflict, missing identities to not-found, missing parents and cross-project
  lineage to constraint. Three threads paginate 2+1 without duplication.
  Identical message retry returns sequence 1, the next append gets sequence 2,
  and changed content under the same key conflicts. Full test-all, lint,
  generation, migration, and artifact checks pass.
Failures-or-discarded-approaches: Corrected the repository test prefix from
  `rep_` to the canonical `repo_`; replaced blanket constraint-to-conflict
  mapping with extended SQLite classification so FK/check failures remain
  constraints while uniqueness failures are conflicts
Known-limitations: Task, event, approval, budget, checkpoint, validation, and
  evidence repositories remain in the next repository slice
Next-safe-step: Implement atomic task creation/transition/event sequence,
  approval resolution, exact budget accounting, checkpoint persistence, and
  validation/evidence operations with idempotent and concurrent tests

Dev-Log: DL-20260730-035
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-034
Request-or-TODO: M03-044, M03-055, M03-059, and M03-060
Goal: Give every repository operation one cancellable atomic write boundary and
  stable failure classifications before implementing domain repositories
Assumptions: SQLite immediate write intent is the correct place to surface
  contention; application code must not receive a generic SQL transaction;
  commit success remains success even if cancellation races afterward
Decisions: Keep SQL access private on the storage Transaction type; roll back
  every callback failure; classify not-found, conflict, stale revision, busy,
  corruption, and constraints; temporarily lower a connection's busy timeout
  when the context deadline is shorter and restore it before pool reuse
Files-or-schemas: internal/storage transaction runner, typed errors and
  real-database tests, docs/storage.md, TODOS.md, CHANGELOG, DEVLOG; no schema
  change
Validation: The initial-schema commit passes local generation, migration,
  integrity, and repository suites. Transaction tests commit one successful
  row, remove both sides of an injected multi-table failure, cancel a writer
  blocked behind a five-second busy policy in about 100 ms, verify policy
  restoration, and match every typed error. Full repository test-all, lint,
  generation, migration, and artifact checks pass.
Failures-or-discarded-approaches: The initial database/sql BeginTx approach
  ignored the Go deadline while the SQLite busy handler waited five seconds.
  Replaced it with a dedicated connection whose busy timeout is bounded to the
  remaining context only for that operation, then restored.
Known-limitations: Domain repository methods are the only intended consumers
  and are introduced next; direct SQL remains private to storage
Next-safe-step: Define operation-oriented repository interfaces and implement
  project, repository, thread, message, task/event, approval, budget,
  checkpoint, validation, and evidence persistence on this runner

Dev-Log: DL-20260730-034
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-033
Request-or-TODO: M03-026 through M03-043
Goal: Materialize the plan's Phase 1 durable authority before repositories can
  persist incompatible or under-constrained records
Assumptions: Unix-microsecond integers are the canonical storage timestamp;
  exact costs use currency plus integer minor units; mutable aggregates use
  optimistic revisions; explicit deletion work must remain possible
Decisions: Add all initial entities in one ordered migration; enforce domain
  state sets and non-negative numeric values in SQLite; use unique idempotency
  and monotonic sequence constraints; index the declared access paths; trigger
  update rejection only for append-only facts and leave aggregate transitions
  to revision-aware repository transactions
Files-or-schemas: migration 000001, generated migration and build catalogs,
  storage schema tests, docs/storage.md, TODOS.md, CHANGELOG, DEVLOG; schema
  version advances from 0 to 1
Validation: Migration/recovery commit passed public CI run 30522494849. A real
  migrated database contains all 26 tables and required indexes. Tests reject
  missing lineage, unknown task state, duplicate message and event sequence,
  negative budget values, task-event and message rewrites; scan every table
  column for credential-bearing names; and pass foreign-key and integrity
  checks. Every committed migration start version upgrades successfully.
Failures-or-discarded-approaches: The first schema run safely restored after an
  index referenced an omitted model-request timestamp; added the lifecycle
  timestamps before regeneration. Updated the original embed test to validate
  an ordered growing catalog rather than assuming exactly one migration.
Known-limitations: Domain repositories and transaction operations are not yet
  implemented; provider-specific usage remains validated JSON until its adapter
  contract is introduced
Next-safe-step: Define the context-cancellable transaction runner, domain
  repository interfaces, typed storage failures, and real-SQLite repository
  implementations and concurrency/idempotency/rollback tests in M03-044 onward

Dev-Log: DL-20260730-033
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-032
Request-or-TODO: M03-012 through M03-025
Goal: Make schema change deterministic, exclusive, recoverable, and auditable
  before the operational tables are introduced
Assumptions: Migration infrastructure tables may be bootstrapped before the
  first application migration; one recovery snapshot covers one ordered startup
  migration batch; interrupted and failed attempts require explicit review
  rather than automatic repeated mutation
Decisions: Use a cross-platform OS file lock beside the database; verify
  embedded and applied SHA-256 checksums; require two database sizes plus a
  fixed 16 MiB free-space reserve; use SQLite Online Backup/Restore; record
  start before execution and update schema version plus success in one SQL
  transaction; restore then record one stable failure on error or interruption
Files-or-schemas: go.mod, go.sum, docs/storage.md, migrations source loader,
  internal/storage backup, disk-space, migration engine and real-database tests,
  TODOS.md, CHANGELOG, DEVLOG; adds migration-control schema at runtime
Validation: The preceding M02 policy commit passed public CI run 30521523866.
  Tests migrate empty databases and dynamically upgrade from every committed
  schema version; verify exclusive lock timeout, disk-space refusal, newer
  schema refusal, source checksum mismatch, online backup restoration,
  transaction rollback, pre-attempt snapshot restoration, interrupted-start
  recovery, and unchanged failure count on repeated startup. Full repository
  test-all, lint, generation, migration, and artifact checks pass.
Failures-or-discarded-approaches: Rejected a durable lock row alone because a
  crashed process would leave ambiguous authority; the OS lock releases on
  process death while durable history records interrupted intent. Rejected raw
  file copying in favor of SQLite's consistency-aware Online Backup API.
Known-limitations: Version 0 remains the only committed application migration;
  the initial operational schema is the next append-only migration
Next-safe-step: Add the normalized operational tables, ownership and lineage
  constraints, idempotency and sequence uniqueness, query indexes, numeric and
  state checks, and immutability enforcement required by M03-026 through
  M03-043

Dev-Log: DL-20260730-032
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-031
Request-or-TODO: M03-001 through M03-011
Goal: Establish the portable, durable, bounded SQLite lifecycle on which all
  migration and repository work depends
Assumptions: The local task journal values committed durability above marginal
  write speed; WAL still has one writer; four pooled connections are a bounded
  starting point for concurrent local reads
Decisions: Pin the CGO-free modernc driver; choose OS-native application-data
  paths; pre-create restrictive directories and files; configure all
  connection-local pragmas in the DSN; use FULL synchronous durability and
  immediate write intent; classify SQLite failures without leaking paths
Files-or-schemas: go.mod, go.sum, docs/storage.md, internal/storage database
  lifecycle and real-database tests, TODOS.md, CHANGELOG, DEVLOG; no schema
  change
Validation: Real-SQLite tests verify Windows-safe URI construction, WAL,
  foreign keys, FULL synchronous mode, configured and observed busy timeout,
  one real read/write transaction rolled back after probing, constraint
  enforcement, pool bounds, file permissions, corrupt content, unreadable
  classification, and idempotent checkpointed close. Full repository test-all,
  lint, generation, and artifact checks pass.
Failures-or-discarded-approaches: The first Windows test caught `file://C:/`
  being parsed as a URI authority; fixed it to a drive-safe `file:///C:/`
  path. Replaced host-native cross-OS path simulation with explicit Windows and
  POSIX path semantics. Rejected CGO and synchronous NORMAL for the declared
  build matrix and durability contract.
Known-limitations: Native Windows ACL hardening belongs to the installer and
  credential milestone; schema migration coordination begins at M03-012
Next-safe-step: Implement embedded forward migrations, schema and migration
  history, exclusive version coordination, backup, disk-space validation,
  downgrade refusal, failure preservation, and restoration tests

Dev-Log: DL-20260730-031
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-030
Request-or-TODO: M02-022 through M02-033 and M02-G01 through M02-G03
Goal: Finish the pure domain value layer with honest unknowns, exact cost
  arithmetic, provider-neutral policy, and mechanically enforced ownership
Assumptions: Correctness is a fixed floor; fast, balanced, and economical
  presets vary latency and escalation posture without lowering that floor;
  currency values use canonical three-letter codes and integer minor units
Decisions: Represent unknown usage and each forecast dimension with explicit
  known flags; keep provider-specific usage categories without losing the
  common categories; make invalid values typed; scan the Go AST in repository
  lint so only internal/domain/states.go can define TaskState
Files-or-schemas: internal/domain policy values and tests, codeflux-dev
  repository checks and tests, TODOS.md, CHANGELOG, DEVLOG; no schema change
Validation: Public CI run 30521094501 passed the preceding lifecycle commit.
  Domain and repository-check tests cover exact addition and overflow,
  currency mismatch, unknown versus zero, token categories, budget and
  percentile ordering, enum separation, model metadata, deterministic JSON,
  task-state ownership, and absence of float32/float64 in policy source. Full
  repository test-all, lint, generation, and artifact checks pass.
Failures-or-discarded-approaches: Corrected an initial vet formatting mismatch
  in the AST test; rejected floating-point prices, implicit unknown-as-zero,
  provider-branded reasoning values, and a documentation-only ownership gate
Known-limitations: Pricing snapshots and budget reservations are persistence
  work in later milestones; forecasts are value contracts, not a router yet
Next-safe-step: Begin M03 with the SQLite driver decision, connection policy,
  pragmas, migration authority, and real-database verification required by the
  plan

Dev-Log: DL-20260730-030
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-029
Request-or-TODO: M02-007 through M02-021
Goal: Make lifecycle ownership and permitted transitions explicit before
  persistence, workers, transport, or UI can invent incompatible states
Assumptions: Retryable task failure remains recoverable, while a failed run is
  terminal for that run identity; completed tasks may move only to explicit
  rolled-back state
Decisions: Keep each machine a distinct string type; centralize only generic
  table validation; return typed errors; require a granted approval state on
  task and command authority boundaries; model unknown command outcome as
  recoverable reconciliation without allowing silent re-execution
Files-or-schemas: internal/domain state machines and tests, TODOS.md,
  CHANGELOG, DEVLOG; no schema change
Validation: Identity commits passed public CI runs 30520606086 and
  30520817287. Domain tests cover every allowed edge, negative cases for all
  eight machines, unknown values, terminal task protection, recoverable
  classification, and all non-granted authority outcomes. Full repository
  test-all, lint, and artifact checks pass.
Failures-or-discarded-approaches: Rejected one shared state enum because task,
  run, and command lifecycles have different ownership and terminal semantics;
  rejected a boolean authority bypass because approval state must be explicit
Known-limitations: Persistence revision checks and approval-ledger binding
  arrive in their later milestones; these validators are pure domain policy
Next-safe-step: Define policy, exact money, usage, budget, forecast, risk,
  assurance, validation-severity, and typed-reason values in M02-022 onward

Dev-Log: DL-20260730-029
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-028
Request-or-TODO: M02-005
Goal: Verify every stable identity survives storage and transport conversion
  without losing its kind or bypassing domain validation
Assumptions: One generated protobuf envelope with an explicit kind enum avoids
  21 structurally identical messages while typed adapter functions preserve
  compile-time distinctions
Decisions: Keep JSON and SQL behavior on the pure domain types; place protobuf
  adapters in internal/transport; reject nil and kind mismatches before calling
  the authoritative domain parser; test deterministic protobuf wire encoding
Files-or-schemas: api/proto/codeflux/v1/identity.proto and generated Go,
  internal/domain tests, internal/transport identity adapters and tests,
  TODOS.md, CHANGELOG, DEVLOG; protobuf schema only
Validation: All 21 kinds pass JSON and SQL round-trips plus generated protobuf
  deterministic marshal/unmarshal and adapter round-trips. Null, zero,
  unspecified, wrong-kind, and malformed inputs fail with typed errors.
  generate-check, repository test-all, lint, and artifact checks pass.
Failures-or-discarded-approaches: Rejected importing generated transport types
  into domain and rejected untagged protobuf strings because either would
  weaken the domain dependency center or identity-kind safety
Known-limitations: These messages are identity primitives only; M07 remains
  authoritative for service contracts
Next-safe-step: Define the task lifecycle states and permitted transitions in
  M02-007 and M02-008 with exhaustive pure-domain tests

Dev-Log: DL-20260730-028
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-027
Request-or-TODO: M02-001 through M02-004 and M02-006
Goal: Establish transport-independent stable identities before storage,
  services, or UI can invent interchangeable strings
Assumptions: Type-prefixed lowercase UUIDv7 provides sufficient local
  uniqueness and chronological sortability without adding a dependency
Decisions: Use a private generic typed core with a distinct kind marker for
  each exported struct; enforce canonical RFC 9562 version and variant bits;
  return typed boundary errors; leave exhaustive protobuf conversion to
  M02-005
Files-or-schemas: internal/domain identities and tests, TODOS.md, CHANGELOG,
  DEVLOG; no persistence schema
Validation: Final M01 gate commit passed public CI run 30520206636 across all
  declared jobs. Domain tests cover all 21 prefixes, invalid format classes,
  JSON/SQL zero rejection, generated UUID parsing, chronological order, and
  non-assignability/non-convertibility; repository test-all and lint pass.
Failures-or-discarded-approaches: Rejected plain string aliases because they
  admit accidental interchange, random UUIDv4 because it is not time-sortable,
  and a third-party identity dependency because the standard library is
  sufficient
Known-limitations: M02-005 remains open until JSON, SQL, and generated-protobuf
  conversions are exercised for every identity kind
Next-safe-step: Add one generated protobuf identity envelope and exhaustive
  adapter round-trip tests without importing transport types into domain

Dev-Log: DL-20260730-027
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-026
Request-or-TODO: M01-G04
Goal: Replace local cross-build confidence with actual hosted execution on
  every declared prototype platform
Assumptions: A successful matrix bound to the pushed commit is the acceptance
  authority for this milestone gate
Decisions: Record the public run URL and full commit identity; require the
  evidence-only follow-up commit to pass CI before starting M02
Files-or-schemas: TODOS.md, CHANGELOG, DEVLOG; no schema change
Validation: GitHub Actions run 30519967041 completed successfully for Windows
  11 ARM64 quality and build, Windows Server 2025 AMD64 build, macOS 15 ARM64
  build, Ubuntu 24.04 AMD64 build, and Ubuntu 24.04 AMD64 race tests at commit
  c8b3f2f2e3cb00a402bb9fb9205c48cda673c246
Failures-or-discarded-approaches: The first public run exposed and preserved a
  Windows cross-volume temp-root failure; CL-20260730-025 fixed the general
  path-classification defect before the successful clean rerun
Known-limitations: Browser E2E remains intentionally unavailable until M16 and
  is not part of the M01 platform gate
Next-safe-step: Commit and push this evidence, require its complete hosted
  matrix to pass, then begin M02-001 from the cited plan sections

Dev-Log: DL-20260730-026
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-025
Request-or-TODO: M01-G04
Goal: Repair the first public hosted-CI failure without weakening path safety
Assumptions: An absolute `--root` on a different Windows volume is external to
  the repository and is an authorized test/output target
Decisions: Treat different Windows volume names as external only when
  `filepath.Rel` cannot compare them; preserve every existing rejection for the
  repository root and repository-local paths outside `.artifacts`
Files-or-schemas: cmd/codeflux-dev root resolution and tests, CHANGELOG,
  DEVLOG; no schema change
Validation: Public CI run 30519748127 passed macOS ARM64, Ubuntu AMD64, Ubuntu
  race, and Windows ARM64 jobs; Windows Server 2025 failed the deterministic
  profile test because the checkout was on `D:` and its temporary root on `C:`.
  The new Windows cross-volume regression and local suites pass.
Failures-or-discarded-approaches: Rejected removing root confinement or
  treating every `filepath.Rel` error as safe; only provably different volumes
  receive the external-root classification
Known-limitations: M01-G04 remains open until a hosted rerun passes every
  declared platform
Next-safe-step: Commit and push the focused fix, then inspect the complete
  hosted matrix before updating M01-G04 evidence

Dev-Log: DL-20260730-025
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-024
Request-or-TODO: User request to E2E test and iteratively refine prompts and
  process with an adversarial subagent until thesis evidence begins to appear
Goal: Make refinement empirical, independently challenged, resistant to
  evaluator leakage and overfitting, and honest about the strength of evidence
Assumptions: "Optimal prompting" means best observed correctness-constrained
  performance for a frozen evaluation stratum, not a global optimum
Decisions: Preserve milestone order; run only the current M01 development
  profile health smoke now; pre-isolate adversarial review from hidden
  acceptance; preregister one general invariant under a frozen envelope; allow
  one held-out confirmation; reject any correctness or safety regression
Files-or-schemas: AGENTS.md, docs/plan.md, TODOS.md, CHANGELOG, DEVLOG; no
  runtime schema
Validation: `codeflux-dev test-all` passed lint, generation drift, fast,
  integration, and security checks; deterministic `run --once --json` served
  and probed loopback `/healthz`, declared the scripted-provider profile,
  created and removed an empty `.sqlite` target, and shut down; it did not
  execute SQLite, a provider request, the agent loop, browser behavior, or
  product E2E; an independent read-only adversarial subagent audited the plan,
  current implementation, evidence thresholds, leakage controls, and stop
  criteria without inspecting the evaluator repository
Failures-or-discarded-approaches: Rejected immediate ReserveFlow prompt tuning
  because the agent loop, browser E2E harness, and dogfood isolation do not
  exist yet and early exposure would contaminate the frozen benchmark. The
  adversarial reviewer rejected the first protocol draft for mislabeling a
  health smoke as E2E, running review before isolation, and permitting hidden
  acceptance to become a tuning oracle; the corrected draft passed re-review.
Known-limitations: Current health-smoke evidence validates only the M01
  development harness and does not support the Codeflux product or compounding
  thesis; M01-G04 still requires actual hosted CI before M02 can begin
Next-safe-step: Obtain explicit authority for a private GitHub remote, run and
  pass hosted M01 CI, then implement milestones in dependency order until the
  M14 agent loop, M22 E2E harness, and M24 controlled dogfood comparison make
  measured prompt refinement legitimate

Dev-Log: DL-20260730-024
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-023
Request-or-TODO: User instruction to remember no JavaScript, HTML, or CSS and
  use GWC
Goal: Persist the frontend implementation boundary for every future agent and
  frontend TODO
Assumptions: The instruction prohibits handwritten or separately maintained
  browser-language source while allowing GWC to produce browser-consumable
  generated projections from Go
Decisions: Record the boundary in repository-wide AGENTS.md and the detailed
  GoWebComponents frontend plan; include TypeScript in the prohibition; enforce
  forbidden tracked extensions in lint; remove the bootstrap CSS and represent
  the pre-M06 frontend honestly as an empty deterministic asset set
Files-or-schemas: AGENTS.md, docs/plan.md, TODOS.md, cmd/codeflux-dev
  repository checks and generator, web/assets packaging, generated version and
  manifest, CHANGELOG, DEVLOG; no schema change
Validation: Agent and plan wording agree; generator tests cover a Go/WASM
  fixture and an absent pre-spike asset directory; tracked source-extension
  scan, fast tests, lint, and generation drift checks pass
Failures-or-discarded-approaches: Initial validation found the tracked
  `web/assets/static/shell.css`; removed it instead of grandfathering a direct
  violation or weakening the user's frontend boundary
Known-limitations: The M06 spike still must identify and pin the exact
  GoWebComponents v5 release before production frontend implementation
Next-safe-step: Complete M01-G04 on hosted runners, then continue milestones in
  dependency order while enforcing the Go/GWC-only frontend boundary

Dev-Log: DL-20260730-023
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-022
Request-or-TODO: User request to keep a supplied OpenAI API key out of Git
Goal: Prevent local environment credential fallbacks from entering commits
Assumptions: The supplied credential must not be copied into repository files,
  commands, diffs, logs, SQLite, or diagnostics
Decisions: Follow docs/plan.md Provider Credentials guidance: prefer the
  operating-system credential store, ignore `.env` variants as an explicit
  fallback, and keep example templates eligible for tracking
Files-or-schemas: .gitignore, CHANGELOG, DEVLOG; no schema change
Validation: `git check-ignore` recognizes `.env`, `.env.local`, and
  `.env.development.local`; example-template probes remain unignored; tracked
  source contains no project-key prefix
Failures-or-discarded-approaches: Did not persist the supplied key because
  doing so would duplicate a secret into repository-adjacent state
Known-limitations: A credential pasted into chat should be rotated at the
  provider; the repository cannot revoke it
Next-safe-step: Rotate the exposed key, store its replacement in Windows
  Credential Manager, then authorize creation and publication of the private
  GitHub repository needed for M01-G04

Dev-Log: DL-20260730-022
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-021
Request-or-TODO: M01-095, M01-G01 through M01-G09 except M01-G04
Goal: Prove the documented M01 contributor journey from tracked source alone
  and distinguish local cross-build confidence from hosted runner evidence
Assumptions: A local no-hardlink Git clone is a valid fresh-clone test for
  repository completeness but cannot substitute for remote hosted CI execution
Decisions: Run every documented prerequisite/build/test/generation/lint/start
  step in the clone; preserve G04 as open rather than converting cross-builds
  into a false CI claim
Files-or-schemas: TODOS.md and ledgers only; no source or schema change
Validation: Clean clone bootstrap/build/test/generate/lint/migration/run/
  benchmark/artifact checks passed and remained Git-clean; all four platform
  cross-builds passed; actionlint previously passed; git remote list is empty
Failures-or-discarded-approaches: None
Known-limitations: M01-G04 requires a GitHub remote and actual hosted runs on
  windows-11-arm, windows-2025, macos-15, and ubuntu-24.04
Next-safe-step: Commit local gate evidence. Do not begin M02 because its
  dependency list includes M01-G04; obtain authority and a remote CI run.

Dev-Log: DL-20260730-021
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-020
Request-or-TODO: M01-063, M01-064, M01-090 through M01-094, M01-097
  through M01-104
Goal: Close the developer-experience safety boundary for command roots, package
  direction, generated paths, artifact confinement, CI scans, and truthful docs
Assumptions: Explicit external output roots may be used but are never selected
  or deleted implicitly; repository-local roots must be .artifacts children
Decisions: Parse imports directly; classify only untracked disposable outputs
  as escapes; add always-run CI scans; implement real M01 microbenchmarks and
  migration validation before documenting them; keep package/browser skeletons
Files-or-schemas: codeflux-dev boundaries, benchmarks, roots, CI, AGENTS.md,
  docs/plan.md, and tests; no runtime schema
Validation: Root matrix, dependency, artifact ignore/escape, cleanup, benchmark,
  migration, actionlint, staged lint, test-all, fast tests, artifact-check, and
  whitespace checks passed
Failures-or-discarded-approaches: Initial combined patch for benchmark registry
  missed shifted context and made no changes; reapplied as smaller exact patches
Known-limitations: Package and browser remain honest future skeletons; actual
  multi-platform hosted CI execution still requires a GitHub remote
Next-safe-step: Complete staged review and commit, then validate documented
  bootstrap/build/test/generation/run commands from a clean local clone.

Dev-Log: DL-20260730-020
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-019
Request-or-TODO: M01-085 through M01-088
Goal: Complete the current-scope command semantics while making later subsystem
  absence, live cost, credential authority, and skipped platform work explicit
Assumptions: M01 may validate live-provider intent but must not access a real
  credential or provider before M04/M12; browser tests remain M16-owned
Decisions: Require os:// credential references and out-of-repository live
  databases; use exit 3 for unavailable subsystems; make test-all include only
  current required local checks; add Buf lint and HEAD compatibility to lint
Files-or-schemas: codeflux-dev live gate, test orchestration, registry, lint,
  and tests; live gate schema version 1; no runtime schema
Validation: Live positive/negative gates, skeleton status tests, Buf lint and
  breaking, integration, security, full current-scope test-all, staged lint,
  fast tests, and whitespace checks passed
Failures-or-discarded-approaches: None
Known-limitations: run-live intentionally sends no request until M04/M12;
  browser remains unavailable until M16; local Windows ARM64 race remains
  unsupported and enforced by Ubuntu AMD64 CI
Next-safe-step: Stage and validate command orchestration, commit it, then make
  root/output and artifact-escape policy uniform across every command.

Dev-Log: DL-20260730-019
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-018
Request-or-TODO: M01-084, M01-089
Goal: Make the deterministic development profile startable and self-checking
  before later storage, provider, coordinator, and frontend implementations
Assumptions: A newly created zero-length `.sqlite` file is the correct
  uninitialized SQLite target before M03 selects and opens it with a Go driver
Decisions: Bind only 127.0.0.1 on port 0; keep credentials and provider script
  in memory; disable proxy use for self-check; own and cancel the server
  goroutine; clean only validated .artifacts children
Files-or-schemas: codeflux-dev profile registry, deterministic loopback runner,
  and tests; run result schema version 1; runtime schema remains 0
Validation: Profile unit cases, real `run --once --json` startup/health/shutdown,
  cleanup inspection, staged aggregate lint, fast tests, and whitespace passed
Failures-or-discarded-approaches: Rejected selecting a SQLite driver in M01
  because M03-001 is the explicit driver decision gate
Known-limitations: The M01 server exposes only health/profile diagnostics and
  reserves the database target; coordinator, storage schema, provider runtime,
  and frontend are added by their governing milestones
Next-safe-step: Stage and validate the profile cluster, commit it, then add
  explicit live-provider gating and the named current-scope test commands.

Dev-Log: DL-20260730-018
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-017
Request-or-TODO: M01-082, M01-083
Goal: Make one deterministic command own every generated source family that
  exists at M01 and compare all of them without mutating reviewed source
Assumptions: The event registry is legitimately empty until M05 introduces
  event directives; frontend generation currently means the embedded source
  manifest, not a nonexistent WASM build
Decisions: Hash migration and frontend inputs with SHA-256; derive schema and
  asset versions; sort all discovered inputs; declare exact generated paths
Files-or-schemas: Generated registry/catalog/manifest/version sources and
  generator/check tests; generator schema 1; runtime schema remains 0
Validation: Generator and checksum tests, staged aggregate lint, isolated
  generate-check, fast suite, and whitespace checks passed
Failures-or-discarded-approaches: Initial Buf argument assembly duplicated the
  `generate` subcommand; corrected before running the generator
Known-limitations: Event output remains empty until event declarations exist;
  GoWebComponents/WASM binding generation waits for the M06 spike
Next-safe-step: Stage generated and source paths, run aggregate checks, inspect
  the complete diff, then commit the generation family.

Dev-Log: DL-20260730-017
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-016
Request-or-TODO: M01-081
Goal: Make a fresh contributor's required Go and generation toolchain explicit,
  patched, repository-local, machine-readable, and safe from provider secrets
Assumptions: GoWebComponents must remain unselected until the mandatory M06-001
  spike; bootstrap verifies that boundary and reports it as deferred
Decisions: Select Go 1.26.5, Buf 1.72.0, protoc-gen-go 1.36.11, and Staticcheck
  2026.1; install tools under a validated .artifacts root; scrub credential-
  shaped environment variables from tool subprocesses
Files-or-schemas: codeflux-dev bootstrap implementation and tests; bootstrap
  result schema version 1; no runtime database schema
Validation: Unit checks, real JSON bootstrap installation/version probes,
  staged aggregate lint, fast tests, and whitespace checks passed
Failures-or-discarded-approaches: None
Known-limitations: The first bootstrap may require network access and took
  approximately 78 seconds on this host; M06-001 must replace the deferred
  GoWebComponents result with the selected exact release
Next-safe-step: Stage and run aggregate checks, commit bootstrap, then expand
  generation beyond protobuf to every currently declared source family.

Dev-Log: DL-20260730-016
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-015
Request-or-TODO: M01-079, M01-080
Goal: Make the complete development command surface discoverable through one
  cross-platform Go entry point before implementing each subsystem binding
Assumptions: Registry availability and machine-output fields must describe the
  current repository truth, including skeleton commands and unsupported JSON
Decisions: Use registry schema v1, stable exits 0/1/2/3, sorted deterministic
  discovery, per-command help, and a shared safe root resolver
Files-or-schemas: codeflux-dev registry, dispatcher, and tests; command registry
  schema version 1; no runtime database schema
Validation: Registry, help, unavailable JSON, unsupported JSON, and root-boundary
  unit tests, staged aggregate lint, fast suite, and whitespace checks passed
Failures-or-discarded-approaches: Initially labeled bootstrap implemented and
  current commands machine-readable before dispatch supported those claims;
  corrected registry metadata to reflect current behavior
Known-limitations: Root overrides and remaining command handlers are declared
  but are completed by M01-081 through M01-104
Next-safe-step: Stage and validate the registry cluster, then implement pinned
  bootstrap and complete generation families.

Dev-Log: DL-20260730-015
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-014
Request-or-TODO: M01-067 through M01-070, M01-073 through M01-076, M01-078
Goal: Reject structurally incomplete atom documentation and misleading or
  retrieval-hostile atom names before later catalog/storage work can admit them
Assumptions: An exact `//codeflux:atom` directive is the smallest deterministic
  source declaration boundary until the M21 catalog owns admission records
Decisions: Parse Go AST comments rather than source regex alone; require schema
  v1 fields exactly once; allow only narrow reason-bearing name exceptions;
  derive display and normalized names from one canonical identifier and atom ID
Files-or-schemas: AGENTS.md directive rules; codeflux-dev atom checks and
  fixtures; no runtime database schema
Validation: Narrow atom fixture, name, derivation, rename, and orphan-marker
  tests, staged aggregate lint, fast suite, and whitespace inspection passed
Failures-or-discarded-approaches: A direct PowerShell wildcard was not expanded
  for gofmt; reran formatting with explicit resolved fixture paths
Known-limitations: The first reviewed production atom example waits for
  M01-071; durable atom/name/documentation revisions remain M21 work
Next-safe-step: Commit the atom admission boundary, then expand codeflux-dev
  into its declared command registry and development profiles.

Dev-Log: DL-20260730-014
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-013
Request-or-TODO: M01-060 through M01-062, M01-065
Goal: Turn repository instruction authority into an offline deterministic lint
  boundary and prove plan/TODO identification precedes a synthetic edit
Assumptions: A revision-pinned HTTPS reference is checked structurally during
  offline lint; its Git revision was separately resolved before tracking
Decisions: Validate only AGENTS.md and CLAUDE.md links at this stage; require
  tracked instruction roots and a compact line-count/section duplication guard
Files-or-schemas: codeflux-dev instruction checks and tests; synthetic text
  smoke fixture; no runtime schema
Validation: Positive and negative instruction tests, staged aggregate lint,
  fast tests, and staged whitespace inspection passed
Failures-or-discarded-approaches: None
Known-limitations: The check does not make network requests during normal lint;
  milestone reviews and real command documentation remain M01-063/M01-064
Next-safe-step: Commit the instruction harness, then implement atom comment and
  name validation with the complete required fixture matrix.

Dev-Log: DL-20260730-013
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-012
Request-or-TODO: M01-055 through M01-059, M01-066, M01-072
Goal: Put the authoritative repository instructions and thin Claude entry point
  under version control before executable instruction checks depend on them
Assumptions: The existing untracked files are the planned M01 instruction
  outputs, not unrelated user work; their checked TODOs identify them exactly
Decisions: Keep AGENTS.md authoritative and CLAUDE.md pointer-only; pin the
  community reference to revision 2c606141936f1eeef17fa3043a72095b4765b9c2
Files-or-schemas: AGENTS.md, CLAUDE.md, TODOS.md, ledgers; no runtime schema
Validation: Required sections, pointers, four disciplines, versioned atom
  template, naming grammar, community attribution, and explicit unofficial
  status were inspected; the pinned Git revision resolves remotely
Failures-or-discarded-approaches: The original reference used a moving
  repository URL and CLAUDE.md did not explicitly state unofficial status;
  both were corrected before tracking
Known-limitations: Automated presence, pointer, link, smoke, atom-comment, and
  atom-name enforcement begin at M01-060
Next-safe-step: Commit the instruction baseline, then add executable checks and
  fixtures without creating additional Markdown files.

Dev-Log: DL-20260730-012
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-011
Request-or-TODO: M01-047 through M01-054
Goal: Enforce repository quality on every declared prototype platform without
  exposing provider credentials or treating cached output as correctness
Decisions: Use GitHub-hosted Windows 11 ARM64 as the primary job and a build
  matrix covering Windows AMD64, macOS ARM64, and Ubuntu AMD64; cache only
  setup-go dependency inputs; upload only an allow-listed JSON failure context
Files-or-schemas: .github workflows and Dependabot policy; codeflux-dev
  failure-artifact helper; no runtime schema
Validation: Failure-artifact unit test proved environment values are excluded;
  fast suite and lint passed; actionlint v1.7.12 accepted the workflow; manual
  failure artifact contained only schema, commit, Go, OS, and architecture
Failures-or-discarded-approaches: actionlint v1.7.7 predated the current
  GitHub-hosted windows-11-arm label and rejected it; current v1.7.12 validates
  the official label
Known-limitations: Hosted execution requires a GitHub remote and is deferred to
  the M01 platform gate; no repository remote is configured
Next-safe-step: Commit the CI cluster, then track and validate the existing
  authoritative AGENTS.md and thin CLAUDE.md instruction files.

Dev-Log: DL-20260730-011
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-010
Request-or-TODO: M01-045, M01-046
Goal: Document only the native prerequisites and regeneration commands that
  are executable at the current repository milestone
Assumptions: The existing docs/plan.md is the authorized documentation
  destination; no root README may be created
Decisions: Separate required host tools from network-fetched pinned generators;
  state explicitly that WASM generation does not exist yet
Files-or-schemas: docs/plan.md, DEVLOG; no schema
Validation: The documented generate-check, lint, and fast-test commands passed;
  the executable plan trace check passed; repository-relative paths resolve
Failures-or-discarded-approaches: None
Known-limitations: Full bootstrap and WASM instructions wait for their actual
  implementation tasks
Next-safe-step: Commit documentation, then add primary-host and declared-platform
  CI in M01-047 through M01-054.

Dev-Log: DL-20260730-010
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-009
Request-or-TODO: M01-042 through M01-044
Goal: Make every development build identify its source, toolchain, schema, and
  frontend version and expose that identity through the CLI
Assumptions: The current commit timestamp is a deterministic development build
  date; absent linker values must display as unknown rather than fabricated
Decisions: Derive ldflags from Git; disable duplicate automatic VCS stamping;
  make doctor return unavailable until required subsystems exist
Files-or-schemas: internal/buildinfo, cmd/codeflux, cmd/codeflux-dev; schema
  version remains 0
Validation: Build-info, CLI, and development-helper tests passed; built
  `codeflux version` reported all required fields with the exact Git short
  commit and commit date; doctor reported working executable/runtime/Git checks
  and returned expected unavailable exit 3 for future subsystems; lint passed
Failures-or-discarded-approaches: Rejected wall-clock build timestamps because
  they would break reproducibility
Known-limitations: Doctor checks only the current executable, Go runtime, and
  Git presence until later subsystem tasks implement deeper probes
Next-safe-step: Commit CLI identity, then add operator documentation to the
  authorized existing plan/agent files for M01-045 and M01-046.

Dev-Log: DL-20260730-009
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-008
Request-or-TODO: M01-036 through M01-041
Goal: Prevent generated drift, invalid migration ordering, committed databases,
  and common provider credentials from entering the repository
Assumptions: Git-tracked files are the review boundary; untracked disposable
  output is separately constrained to .artifacts
Decisions: Generate protobuf output into a validated .artifacts/tmp child and
  compare exact trees; run repository checks as part of lint; scan text only
Files-or-schemas: cmd/codeflux-dev checks and tests; no runtime schema
Validation: Repository-check unit tests passed their positive and negative
  cases; isolated `generate-check`, `lint`, build, and pre-commit hook passed;
  the generation staging child was removed and no output escaped .artifacts
Failures-or-discarded-approaches: Rejected in-place regeneration for checks
  because a stale result would dirty or overwrite the user's worktree; direct
  `sh` was not on the PowerShell PATH, so hook verification uses Git's
  cross-platform `git hook run` entry point
Known-limitations: The initial secret patterns are a defense-in-depth baseline,
  not proof that arbitrary secrets are detectable; CI enforcement is M01-050
Next-safe-step: Commit repository integrity, then add structured version
  metadata, `codeflux version`, and the doctor skeleton in M01-042 through
  M01-044.

Dev-Log: DL-20260730-008
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-007
Request-or-TODO: M01-034, M01-035
Goal: Establish compile-time embedding for ordered SQL migration inputs and
  deterministic frontend assets
Assumptions: A numbered no-op bootstrap SQL file establishes the mechanism
  without pre-implementing M03 schema; a minimal CSS asset proves frontend
  packaging without claiming a finished shell
Decisions: Expose read-only embedded filesystems; keep assets as reviewed
  source; test exact paths and non-empty content
Files-or-schemas: migrations embedded SQL, web/assets embedded CSS, package
  tests; no application schema
Validation: gofmt passed; migration and asset package tests read the exact
  embedded paths and non-empty content; full build, fast suite, vet, and
  Staticcheck passed
Failures-or-discarded-approaches: None
Known-limitations: Migration execution begins in M03; WASM generation and the
  GoWebComponents shell remain later tasks
Next-safe-step: Commit embedding, then add generated-file, regeneration,
  migration-order, database-artifact, and secret checks in M01-036 through
  M01-041.

Dev-Log: DL-20260730-007
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-006
Request-or-TODO: M01-033
Goal: Add real, version-pinned protobuf generation without requiring a global
  protoc installation
Assumptions: A pinned `go run` invocation of Buf 1.72.0 plus the pinned remote
  protocolbuffers/go 1.36.11 plugin is reproducible for the frozen schema
Decisions: Use Buf's internal compiler and remote Go plugin; commit generated
  Go and its runtime dependency; keep generation explicitly network-capable
Files-or-schemas: buf.yaml, buf.gen.yaml, api/proto/codeflux/v1/system.proto,
  generated api/gen source, cmd/codeflux-dev, go.mod, go.sum
Validation: Buf lint passed; two generations produced identical SHA-256
  E9E6331D9AD000D782C80EB91663B2CA2DA670D2FB09FBBEC4DAEC639FEE5280;
  the generated header is present; module graph tidied to protobuf runtime
  v1.36.11; build, fast tests, vet, and Staticcheck passed
Failures-or-discarded-approaches: Rejected retaining Buf as a Go tool directive
  because it expanded the application module graph with the CLI's large
  transitive dependency set; use a version-qualified go run tool instead
Known-limitations: First generation requires network access to fetch the pinned
  Buf command and remote plugin; clean-clone bootstrap will make that explicit
Next-safe-step: Commit protobuf generation, then add migration embedding and
  deterministic frontend asset packaging for M01-034 and M01-035.

Dev-Log: DL-20260730-006
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-005
Request-or-TODO: M01-028 through M01-032
Goal: Enforce formatting, vet, low-noise static analysis, race coverage, and
  unit coverage through the repository development helper
Assumptions: Staticcheck 2026.1 already installed on the host is usable without
  dependency installation; race coverage will execute on a supported CI host
Decisions: Pin staticcheck 2026.1; verify Go formatting in-process; store
  coverage only in .artifacts/coverage; report unsupported race hosts clearly
Files-or-schemas: cmd/codeflux-dev, DEVLOG; no schema
Validation: Helper unit tests passed; `lint` passed formatting, vet, and
  Staticcheck 2026.1; `test-coverage` passed and wrote only
  .artifacts/coverage/unit.out; `test-race` returned the expected explicit
  unsupported-host failure on Windows ARM64; artifact escape scan was empty
Failures-or-discarded-approaches: Direct `go test -race ./...` is unsupported
  on the current windows/arm64 host with CGO disabled; this is a declared host
  limitation, not a passing race result
Known-limitations: CI race execution remains governed by M01-051; the current
  repository has no production concurrency-owning package behavior yet
Next-safe-step: Commit quality enforcement, then add pinned protobuf,
  migration-catalog, and frontend generation beginning with M01-033.

Dev-Log: DL-20260730-005
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-004
Request-or-TODO: M01-026, M01-027
Goal: Provide one cross-platform local command for a complete build and one for
  the fast test suite, with all repository-local output under .artifacts
Assumptions: At this dependency point no protobuf or frontend generator source
  exists; compiling all packages is the extension point that will include
  generated Go and embedded frontend assets when their later TODOs add them
Decisions: Add only build and test-fast plus help; build all packages before
  producing codeflux and codeflux-worker binaries in .artifacts/bin
Files-or-schemas: cmd/codeflux-dev, cmd/codeflux-worker, DEVLOG; no schema
Validation: `go test ./cmd/codeflux-dev -count=1` passed; direct `build` emitted
  codeflux.exe and codeflux-worker.exe under .artifacts/bin; direct `test-fast`
  passed all packages; `go vet ./...` passed; the artifact-escape scan found no
  repository-local disposable binary or database outside .artifacts
Failures-or-discarded-approaches: Rejected installing missing protoc or other
  dependencies before the authorized generator tasks
Known-limitations: Generation, lint, bootstrap, profiles, and advanced
  development commands remain unavailable until their own M01 tasks; the
  command currently uses the invoking Go toolchain
Next-safe-step: Commit the command pair, then add formatting, vet, linter, race,
  and coverage enforcement beginning with M01-028.

Dev-Log: DL-20260730-004
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-003
Request-or-TODO: M01-007 through M01-025
Goal: Create the compile-clean package skeleton and make dependency direction
  explicit before behavioral code begins
Assumptions: Package documentation files are sufficient to establish otherwise
  empty package boundaries without inventing behavior or abstractions
Decisions: Keep domain at the dependency center; application orchestration may
  depend on domain ports; adapters may depend inward; commands assemble
  dependencies; no package may depend on frontend presentation
Files-or-schemas: Planned cmd, internal, web, api/proto, migrations, and
  testdata source paths; no runtime schema
Validation: gofmt completed; `go list ./...` found 19 packages and the package
  documentation query found no gaps; `go build` wrote only
  .artifacts/bin/codeflux.exe; `go test ./...` and `go vet ./...` passed
Failures-or-discarded-approaches: None
Known-limitations: Packages intentionally contain documentation only until
  their governing milestones introduce behavior; cmd/codeflux has no command
  surface until M01-043 and later CLI tasks
Next-safe-step: Commit the package skeleton, then implement the cross-platform
  development helper and quality command graph beginning with M01-026.

Dev-Log: DL-20260730-003
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-002
Request-or-TODO: M01-001 through M01-006
Goal: Establish the smallest buildable repository identity and text policy
Assumptions: No Git remote establishes a canonical import path, so
  codeflux.dev/codeflux is used as a reversible product-owned vanity path
Decisions: Set minimum language semantics to Go 1.26.0; require bootstrap and
  CI to select the latest patched 1.26 toolchain; choose Apache-2.0; retain the
  explicitly required absence of a root README
Files-or-schemas: go.mod, LICENSE, .gitattributes, .gitignore, TODOS.md,
  CHANGELOG, DEVLOG; no runtime schema
Validation: `go env GOMOD` resolved the root go.mod; `go mod edit -json`
  reported module codeflux.dev/codeflux and Go 1.26.0; `git check-attr`
  confirmed LF text, CRLF batch files, and binary image treatment;
  `git check-ignore` confirmed .artifacts descendants are ignored; root
  README.md is absent; `git diff --check` passed
Failures-or-discarded-approaches: Rejected pinning the installed Go 1.26.3
  patch because official release history identifies Go 1.26.5 as the current
  security-patched release; rejected inventing a GitHub owner without a remote
Known-limitations: The current host runs Go 1.26.3. M01 bootstrap must report
  the outdated patch and CI must use the latest 1.26 patch. No package exists
  until M01-007.
Next-safe-step: Verify and commit the repository identity, then implement the
  dependency-directed package skeleton in M01-007 through M01-025.

Dev-Log: DL-20260730-002
Date: 2026-07-30
Status: Complete
Change-ID: CL-20260730-001
Request-or-TODO: M00-001 through M00-G05; user requested completion of all
  Codeflux TODOs in dependency order
Goal: Freeze a bounded, independently evaluable prototype contract before
  repository implementation begins
Assumptions: The first internal build may target Windows 11 x64 while portable
  interfaces and declared cross-platform release gates remain mandatory; the
  ReserveFlow scaffold and evaluator will remain sibling repositories
Decisions: Work starts at the first incomplete dependency-safe task; current
  provider/model/pricing facts are verified from official provider sources;
  deferred and research tracks remain outside the prototype critical path
Files-or-schemas: docs/plan.md, docs/check-plan-trace.ps1, TODOS.md, DEVLOG,
  CHANGELOG; separate reserveflow and reserveflow-evaluator Git repositories;
  no runtime schema
Validation: Plan trace check passed for M00-M24, Layers 0-20, referenced plan
  sections, and the subsystem declaration rule; evaluator self-containment
  test passed; Task 1 black-box test failed as expected against the frozen empty
  scaffold because no server is implemented; both fixture repositories are
  clean and pinned to recorded revisions
Failures-or-discarded-approaches: OpenAI Developer Docs MCP was not available
  in the active session; installed the official server for future sessions and
  used official OpenAI web documentation as the bounded fallback
Known-limitations: The PowerShell plan check is an allowed Windows-first
  bootstrap check; M01 must move equivalent enforcement into the planned
  cross-platform Go development helper. The frozen Task 1 evaluator remains
  red until the evaluated Codeflux journey implements the visible contract.
Next-safe-step: Begin M01-001 by selecting and initializing the Go module,
  preserving the frozen M00 contract and platform boundaries.

Dev-Log: DL-20260730-001
Date: 2026-07-30
Status: Uncommitted
Change-ID: None until a commit is authorized
Request-or-TODO: User requested persistent commit and implementation ledgers
Goal: Establish CHANGELOG and DEVLOG governance plus atomic commit discipline
Assumptions: Extensionless tracked ledgers satisfy the prior prohibition on
  creating unspecified Markdown files
Decisions: Bind ledger entries to commits with stable IDs and commit trailers;
  keep implementation chronology separate from completed commit outcomes
Files-or-schemas: AGENTS.md, CHANGELOG, DEVLOG
Validation: Confirmed all three required AGENTS rules are present; CHANGELOG
  and DEVLOG are not ignored; no new Markdown file was created; no known build
  artifact exists outside .artifacts; no trailing whitespace was found
Failures-or-discarded-approaches: Rejected embedding the final commit hash in
  the same commit because changing the file would change that hash
Known-limitations: No commit exists yet and this request does not itself
  authorize creating one
Next-safe-step: Validate the rules and ledgers; add a CHANGELOG entry only when
  an atomic commit is explicitly authorized
