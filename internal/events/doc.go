// Package events implements durable session event append, replay, and
// publication contracts. Durable facts commit with their owning state change;
// live publication follows successful commit.
package events
