// Package storage implements Codeflux repositories and transactions over the
// authoritative local SQLite database. It depends inward on domain contracts
// and does not own presentation or provider policy.
package storage
